# 🏦 خطة الارتقاء بالتطبيق إلى مستوى مصرفي (Bank-Grade)

> ## ✅ الحالة: **نُفِّذت المبادرات الثلاث بالكامل**
>
> | # | المبادرة | الـ commit | الاختبارات |
> |---|---|---|---|
> | 1 | إزالة الباب الخلفي من حزم الإنتاج | `80cc4ec` | 9 |
> | 2 | التشفير المغلَّف (إصلاح فقدان البيانات) | `ae9c97f` | 16 |
> | 3 | انحراف الفاصلة العائمة عند نقاط القرار | `0b33183` | 23 |
>
> **البوابة النهائية:** `tsc` 0 · `eslint` 0 · `vite build` 0 · **488/488 اختبارًا** (440 أساس + 48 جديدًا).
> **معيار القبول الحاسم:** `grep -r "MASTER_HASH" dist/` ← **صفر نتائج**.
> تفاصيل التنفيذ في `BANK_GRADE_IMPLEMENTATION.md`.

**التاريخ:** 13 سبتمبر 2025 · **الأساس:** commit `65f5c89`
**المنهج:** لم أرشّح شيئًا بالانطباع. كل بند أدناه **مُثبَت بتشغيل فعلي** على الكود، والدليل مرفق.

---

## 0. كيف اخترتُ هذه الثلاثة

رتّبتُ المرشحين بمعيارين: **هل يفقد المستخدم ماله أو بياناته؟** و**هل الخلل موجود فعلًا لا نظريًا؟**

استبعدتُ مرشحين يبدون جذابين لكنهم أقل خطورة عمليًا:

| مرشّح مستبعَد | لماذا استبعدته |
|---|---|
| تشفير الحقول يكسر فهارس Dexie | بحثتُ عن استعلام واحد يرشّح أو يرتّب بحقل مشفَّر: **صفر نتائج**. المشكلة نظرية هنا. |
| ثغرة `advance()` في وكيل المؤشر | حقيقية، لكن **لا مسار كود يستدعيها** (`.offset()` غير مستخدم). دين تقني لا ثغرة نشطة. |
| CSP متساهلة / OAuth بلا PKCE | مهمة، لكن أثرها أقل من فقدان بيانات مؤكد. |

الثلاثة المختارة **كلها مؤكدة بالتشغيل**.

---

# 🥇 الأولوية الأولى: إصلاح فقدان البيانات عند تغيير الـ PIN

## المشكلة — وهذه ليست نظرية، أثبتُّها

**تغيير رمز الـ PIN يُتلف كل البيانات المشفّرة إتلافًا دائمًا لا رجعة فيه.**

كتبتُ اختبارًا يُحاكي السيناريو بالضبط وشغّلتُه، و**نجح في إثبات العطل**:

```ts
// 1. المستخدم يضع PIN = 1111 → ملح A → مفتاح A
setEncryptionKey(await deriveMasterKey('1111', saltA));
const blob = await encryptData({ amount: 25000, description: 'Salary' });
// ✅ يُقرأ بنجاح

// 2. يغيّر الـ PIN إلى 2222 → SecurityCard يولّد ملحًا جديدًا B
setEncryptionKey(await deriveMasterKey('2222', saltB));

// 3. النتيجة:
await expect(decryptData(blob)).rejects.toThrow();   // ← نجح الاختبار
```

### السبب الجذري

في `SecurityCard.tsx` سطر 91، دالة `handleSavePin` تفعل هذا:

```ts
const salt = generateSalt();              // ← ملح جديد كليًا
const hashed = await hashPin(newPin, salt);
await DB.setSetting('pinHash', hashed);
await DB.setSetting('pinSalt', salt);
```

ولا تفعل **أيًا** مما يلي: لا تُعيد اشتقاق مفتاح التشفير، ولا تُعيد تشفير أي سجل، ولا تُحذّر المستخدم.

والمفتاح يُشتق مباشرة من `(PIN + salt)` عبر `deriveMasterKey`. فتغيير أيٍّ منهما **يُنتج مفتاحًا مختلفًا تمامًا**، والبيانات القديمة مشفّرة بالمفتاح القديم الذي لم يعد له وجود.

### حجم الكارثة

**12 جدولًا** تصبح غير قابلة للقراءة:
`transactions`, `budgets`, `goals`, `debts`, `bills`, `accounts`, `subscriptions`, `investments`, `installments`, `cards`, `auditLog`, `chatHistory`

أي: **كل المعاملات، كل الأرصدة، كل الأهداف، كل الديون، بيانات البطاقات** — تاريخ المستخدم المالي بأكمله.

> **لماذا هي الأولوية القصوى بلا منازع:** المستخدم يظن أنه يُحسّن أمنه (يغيّر رمزه الدوري كما تنصح كل إرشادات الأمان)، فيُدمّر بياناته. الضرر **صامت** — لن يكتشفه إلا لاحقًا، بعد فوات أوان النسخة الاحتياطية. وهذا بالضبط ما لا يُغتفر في تطبيق مالي.

> **ملاحظة مهمة:** هذا هو **الأثر العملي للتشفير المغلّف الخامل** الذي رصدتُه في التدقيق السابق. آنذاك وصفتُه بأنه «وعد غير مُفعَّل»؛ الآن أثبتُّ أنه **عطل نشط يُفقد البيانات**، لا مجرد ميزة ناقصة.

## الحل: تفعيل التشفير المغلّف (Envelope Encryption)

الدوال **موجودة بالفعل وجاهزة** في `crypto.ts` ولا يستدعيها أحد:

```ts
generateMasterDataKey()          // توليد MDK عشوائي
encryptKey(keyToEncrypt, kek)    // تغليف MDK بمفتاح مشتق من PIN
decryptKey(encryptedKeyB64, kek) // فك التغليف
```

**الفكرة المعمارية:** تُشفَّر البيانات بمفتاح عشوائي ثابت (MDK) **لا يتغير أبدًا**، ويُخزَّن هذا المفتاح نفسه مغلَّفًا بمفتاح مشتق من الـ PIN (KEK). عند تغيير الـ PIN **نُعيد تغليف المفتاح فقط** — عملية واحدة على بضعة بايتات — بدل إعادة تشفير آلاف السجلات.

```
الحالي:  PIN → KEK → تشفير البيانات مباشرةً   ← تغيير PIN = فقدان كل شيء
المقترح: PIN → KEK → يغلّف MDK → MDK يشفّر البيانات   ← تغيير PIN = إعادة تغليف فقط
```

### خطة التنفيذ

| # | الخطوة | التفصيل |
|---|---|---|
| 1 | `vaultKeyStore.ts` جديد | يدير دورة حياة MDK: توليد، تغليف، فك، تخزين `wrappedMDK` في الإعدادات |
| 2 | تعديل مسار إنشاء PIN | عند أول PIN: ولّد MDK، غلّفه بـ KEK، خزّن `wrappedMDK` |
| 3 | تعديل مسار فك القفل | `PinScreen` يفكّ `wrappedMDK` بـ KEK ثم يضع **MDK** كمفتاح فعّال (لا KEK) |
| 4 | **تعديل `handleSavePin`** ← الإصلاح الجوهري | فك MDK بالـ KEK القديم → إعادة تغليفه بالجديد → حفظ. **بلا لمس أي سجل بيانات** |
| 5 | **ترحيل آمن للمستخدمين الحاليين** | الأصعب: بياناتهم مشفّرة بالنمط القديم |
| 6 | اختبارات | تغيير PIN مرتين متتاليتين والبيانات تبقى مقروءة + الترحيل + الفشل الآمن |

### الخطوة 5 بتفصيل — لأنها الأخطر

لا يجوز أن يفقد مستخدم حالي بياناته أثناء الترحيل نفسه. الخوارزمية:

1. عند فك القفل بعد التحديث، إن وُجد `pinSalt` ولم يوجد `wrappedMDK` → المستخدم على النمط القديم.
2. اشتق المفتاح القديم (KEK) — **به تُقرأ البيانات الحالية**.
3. **اجعل MDK الجديد = المفتاح القديم نفسه**، ثم غلّفه بـ KEK وخزّنه.
4. النتيجة: **صفر إعادة تشفير، صفر مخاطرة** — البيانات تبقى كما هي، وما تغيّر هو *طريقة الوصول* للمفتاح.
5. من هذه اللحظة، أي تغيير PIN لاحق يُعيد التغليف فقط.

هذه أنظف صورة للترحيل: لا نلمس بيانات المستخدم إطلاقًا.

### معايير القبول
- ✅ تغيير الـ PIN مرتين متتاليتين والبيانات تبقى مقروءة
- ✅ مستخدم قديم يُرحَّل تلقائيًا بلا فقد
- ✅ فشل فك التغليف = **رفض** لا كتابة بنص صريح (fail-closed)
- ✅ اختبار انحدار يفشل لو عاد أحد لاشتقاق المفتاح من PIN مباشرة

**التقدير:** 2–3 أيام · **الخطورة:** عالية (تمسّ التشفير) → خلف راية تفعيل + نسخة احتياطية إجبارية قبل الترحيل.

---

# 🥈 الأولوية الثانية: إغلاق الباب الخلفي للمطوّر

## المشكلة

في `Settings.tsx`: سبع نقرات على رقم الإصدار تفتح نافذة، وكلمة سر صحيحة تستدعي `unlockEverything()`.

فحصتُ التنفيذ فوجدتُ ثلاث مشاكل متراكبة:

**١. الباب يُشحن في نسخة الإنتاج.** لا يوجد أي `import.meta.env.DEV` حوله. أي مستخدم يعرف الحيلة يصل إلى النافذة.

**٢. المقارنة غير ثابتة الزمن:**
```ts
if (inputHash === targetHash) {
```
مقارنة `===` على السلاسل تخرج عند أول بايت مختلف، وهي عرضة نظريًا لهجوم توقيت. والمفارقة أن المشروع **يملك `timingSafeEqual` ويستخدمها في `PinScreen`** — لكن لا هنا.

**٣. التجزئة المستهدفة تُدمَج في الحزمة.** `VITE_MASTER_HASH` متغير `VITE_*`، و Vite **يستبدله نصيًا في كود العميل**. فهو قابل للاستخراج من أي APK بأمر `strings`. صحيح أنه PBKDF2 بـ600 ألف دورة (كسره مكلف)، لكن **المبدأ المصرفي: لا يُشحن سر إلى العميل أصلًا**.

ما يمنحه الباب: 99,999 نقطة + كل الثيمات + كل الميزات المدفوعة.

> **لماذا الثانية لا الأولى:** لا يُفقد بيانات، وأثره اقتصادي (تجاوز المشتريات) وسمعي. لكنه **ألزم ما يكون قبل أي تدقيق أمني خارجي** — وجود باب خلفي في نسخة الإنتاج بند إخفاق فوري في أي مراجعة مصرفية.

## الحل

**الخيار الموصى به: حذفه من الإنتاج نهائيًا.**

```ts
if (import.meta.env.DEV) {
  // الباب متاح للتطوير فقط
}
```
مع `import.meta.env.DEV` يقوم Vite بـ**tree-shaking** فيختفي الكود والسر من حزمة الإنتاج تمامًا — لا مجرد تعطيل.

إن كان مطلوبًا لأغراض الدعم الفني، فالبديل الصحيح: **تحقّق من الخادم** (رمز لمرة واحدة محدود الصلاحية) لا سر مدمَج.

### خطوات التنفيذ
1. تغليف `handleMasterPass` و`onVersionClick` والنافذة بـ `import.meta.env.DEV`
2. استبدال `===` بـ `timingSafeEqual` الموجودة
3. إزالة `VITE_MASTER_HASH`/`VITE_MASTER_SALT` من `.env.example` مع توثيق السبب
4. **اختبار انحدار على الحزمة نفسها**: `grep` على `dist/` للتأكد أن السر اختفى
5. تسجيل أي محاولة استخدام في `auditLog`

### معايير القبول
- ✅ `grep -r "MASTER_HASH" dist/` = صفر نتائج
- ✅ المقارنة ثابتة الزمن
- ✅ الميزة تعمل في `npm run dev` فقط

**التقدير:** 3–4 ساعات · **الخطورة:** منخفضة جدًا · **أعلى عائد لكل ساعة عمل في الخطة كلها.**

---

# 🥉 الأولوية الثالثة: تمثيل المال بأعداد صحيحة (Integer Cents)

## المشكلة

كل المبالغ مخزَّنة `number` — أي **IEEE-754 double**. وهذا في الأنظمة المالية خطأ معماري معروف.

الدليل من تشغيل فعلي:
```
0.1 + 0.2      = 0.30000000000000004
0.1 × عشر مرات = 0.9999999999999999   → === 1 ? false
```

والخطورة أن الكود يراكم الأرصدة تراكميًا في `transactions.ts` سطر 16:
```ts
acc.balance += delta;   // بلا أي تقريب
```

قياس الانحراف بعد 1000 معاملة واقعية:
```
عائم : 7290.000000000033
صحيح : 7290
الفارق: 3.27e-11
```

### تقييم صادق للأولوية

**الانحراف صغير جدًا** (جزء من تريليون بعد 1000 معاملة) ولن يُظهر للمستخدم رقمًا خاطئًا بعد التقريب للعرض. **لذلك وضعتُها ثالثة لا أولى** — ولن أبالغ في تهويلها.

لكنها تستحق العلاج لثلاثة أسباب حقيقية:
1. **المقارنات تفشل صامتة:** `if (balance === target)` قد تعطي `false` رغم التساوي المنطقي — وهذا يكسر منطق «اكتمل الهدف» و«سُدِّد الدين بالكامل».
2. **التراكم أُسّي مع الزمن:** حساب عمره سنوات بعشرات آلاف المعاملات يضخّم الانحراف.
3. **معيار مصرفي:** أي مدقق مالي سيعترض على تخزين النقود في float. الأنظمة الجادة تستخدم أعدادًا صحيحة (Stripe يخزّن السنتات).

## الحل المرحلي (بلا ترحيل مخاطر)

لا أنصح بترحيل قاعدة البيانات كاملة إلى سنتات الآن — مخاطرة عالية مقابل عائد محدود. الأفضل **نهج تدريجي**:

| المرحلة | العمل |
|---|---|
| 1 | `src/core/money.ts`: دوال `toCents`/`fromCents`/`addMoney`/`subMoney`/`roundMoney` مع اختبارات |
| 2 | تطبيقها على **نقاط التراكم الحرجة فقط**: `applyBalanceDelta`، تجميع الميزانيات، تقدّم الأهداف، رصيد الديون |
| 3 | استبدال كل `===` على المبالغ بـ `isEqualMoney(a, b)` بهامش نصف سنت |
| 4 | (لاحقًا، اختياري) ترحيل التخزين إلى سنتات صحيحة مع إصدار مخطط جديد |

المرحلتان 1–2 تلتقطان **معظم الفائدة بأقل مخاطرة**.

### معايير القبول
- ✅ 10,000 معاملة تراكمية تُنتج رصيدًا مطابقًا للحساب الصحيح بالضبط
- ✅ «اكتمل الهدف» يعمل عند التساوي الفعلي
- ✅ لا انحدار في الاختبارات الـ440

**التقدير:** المرحلتان 1–2 يومان · **الخطورة:** متوسطة

---

## 📋 الجدول المقترح

| # | البند | المدة | الخطورة | العائد |
|---|---|---|---|---|
| 🥈 | إغلاق الباب الخلفي | 4 ساعات | منخفضة | فوري — **ابدأ به** |
| 🥇 | التشفير المغلّف | 2–3 أيام | عالية | يمنع فقدان بيانات مؤكد |
| 🥉 | حساب المال بالسنتات | يومان | متوسطة | صحة حسابية طويلة الأمد |

**ترتيب التنفيذ الموصى به:** ابدأ بالباب الخلفي (ساعات قليلة، مخاطرة تكاد تكون معدومة، ويُغلق بندًا يُفشل أي تدقيق)، ثم التشفير المغلّف وهو الأهم والأخطر فيستحق تركيزًا كاملًا، ثم المال.

---

## ⚠️ ثلاث ملاحظات بصراحة مهنية

**١. هذه ليست كل ما يلزم لمستوى مصرفي.** طلبتَ ثلاثة فاخترتُ الأعلى أثرًا، لكن يبقى خارج النطاق: C-1 (البناء لم يُختبر فعليًا بـ`gradlew`)، و**Crashlytics غير مُفعَّل** فأنت أعمى تمامًا عن أعطال الإنتاج، وOAuth بلا PKCE، وCSP المتساهلة، وكشف الروت السطحي. **Crashlytics تحديدًا** أقرب المرشحين للمركز الرابع — لا يُعقل أن يكون تطبيق مالي في الإنتاج بلا رؤية لأعطاله.

**٢. الأولوية الأولى تمسّ التشفير — وهذا أخطر ما يُلمس.** خطأ في تنفيذها يُحوّل عطلًا محتملًا إلى فقدان فوري للبيانات. أوصي بشدة: راية تفعيل + نسخة احتياطية إجبارية قبل الترحيل + اختبارات الترحيل قبل أي سطر إنتاج.

**٣. «Bank-Grade» ليست وصفًا تقنيًا فقط.** المصارف تُلزم أيضًا بتدقيق خارجي مستقل، واختبار اختراق، وسجلات تدقيق غير قابلة للتعديل، وخطة استجابة للحوادث. ما أقترحه يرفع **جودة الهندسة** إلى المستوى المطلوب، لكن الوصف الرسمي يحتاج اعتمادًا خارجيًا لا يمنحه الكود وحده.

---

**جاهز للبدء فور اختيارك.** أرشّح البدء بالباب الخلفي اليوم — ساعات قليلة ومكسب فوري — ثم ننتقل للتشفير المغلّف بتركيز كامل.
