# 🏦 تقرير تنفيذ خطة المستوى المصرفي

**التاريخ:** 13 سبتمبر 2025 · **الأساس:** `35b08a9` ← **النهاية:** `0b33183`
**المرجع:** `BANK_GRADE_ROADMAP.md`

نُفِّذت المبادرات الثلاث بالترتيب الذي طلبتَه. هذا التقرير يوثّق **ما تغيّر فعليًا**، و**كيف تحقّقتُ منه**، و**ما لم أفعله ولماذا**.

---

## الملخّص التنفيذي

| # | المبادرة | الـ commit | ملفات جديدة | اختبارات |
|---|---|---|---|---|
| 1 | إزالة الباب الخلفي من الإنتاج | `80cc4ec` | `tests/unit/backdoorRemoval.test.ts` | 9 |
| 2 | التشفير المغلَّف (Key Wrapping) | `ae9c97f` | `src/core/security/vaultKey.ts` · `tests/unit/vaultKey.test.ts` | 16 |
| 3 | انحراف الفاصلة العائمة | `0b33183` | `src/core/money.ts` · `tests/unit/money.test.ts` | 23 |

**البوابة النهائية:** `tsc --noEmit` 0 · `eslint` 0 · `vite build` 0 · **488/488 اختبارًا** في 76 ملفًا.
الأساس كان 440 اختبارًا، أضفتُ 48، ولم يسقط أي اختبار قائم.

---

## مصفوفة معايير القبول

كل معيار اتّفقنا عليه في الخطة، وكيف أُثبت:

| المعيار | الحالة | الدليل |
|---|---|---|
| `grep -r "MASTER_HASH" dist/` لا يُرجع شيئًا | ✅ | نُفِّذ على بناء إنتاج فعلي — صفر نتائج |
| الباب الخلفي يعمل في `npm run dev` فقط | ✅ | حُرِس بـ `import.meta.env.DEV`؛ حزمة `Settings` لا تحوي `masterPass` ولا `showMasterModal` ولا `99999` |
| مقارنة ثابتة الزمن | ✅ | `timingSafeEqual` مشترك؛ اختبار يسقط إذا عاد `===` |
| تغيير الـ PIN مرّتين متتاليتين والبيانات تُقرأ | ✅ | اختبار صريح في `vaultKey.test.ts` |
| ترحيل المستخدمين القدامى بلا فقدان | ✅ | 3 اختبارات: ترحيل، تغيير PIN بعده، وعدم تكرار الترحيل |
| فشل فكّ التغليف = رفض (fail-closed) | ✅ | 5 اختبارات؛ `getEncryptionKey()` يبقى `null` |
| اختبار يسقط إذا اشتُقّ مفتاح البيانات من الـ PIN | ✅ | اختبار على مستوى المصدر يفحص مواقع فكّ القفل الثلاثة |
| لا انحدار في الـ 440 اختبارًا | ✅ | 488/488 |

---

## 🥇 المبادرة 1 — الباب الخلفي

### ما كان موجودًا

نافذة «كلمة مرور رئيسية» تُفتح بالنقر على رقم الإصدار سبع مرّات، تمنح 99,999 نقطة وكل المزايا المدفوعة والـ 14 لوحة ألوان والـ 16 إنجازًا واشتراك ذكاء اصطناعي لعشر سنوات.

خللان جعلا هذا غير مقبول في تطبيق منشور:

1. المصادقة كانت على `VITE_MASTER_HASH` / `VITE_MASTER_SALT`، و**Vite يزرع قيم البيئة كنصوص حرفية داخل الحزمة**. أي شخص يفكّ APK يقرأ البصمة والملح ويهاجمهما بلا حدود وبلا إنترنت.
2. المقارنة كانت `===`، وهي تتوقّف عند أول بايت مختلف، فتُسرّب زمنيًا مقدار ما خُمِّن صحيحًا.

### ما فعلتُه

**حذف، لا تعطيل.** كل فرع من الميزة مُغلَّف بـ `import.meta.env.DEV`. Vite يستبدلها بـ `false` نصيًا في بناء الإنتاج، فيتحوّل الفرع إلى كود ميت ويُزيله tree-shaking ومعه النصوص الحرفية للأسرار.

الفرق جوهري: التعطيل يُبقي الشيفرة والسرّ في الحزمة ويعتمد على شرط وقت التشغيل يمكن تجاوزه بالتعديل على الحزمة. الإزالة تعني أن الميزة **غير موجودة** في الملف المُوزَّع.

كذلك:
- `timingSafeEqual` نُقلت إلى `src/core/security/crypto.ts` كدالّة مُصدَّرة واحدة، وكانت مكرّرة محليًا في `PinScreen.tsx`. ترفض المدخلات غير النصية صراحةً، حتى لا يصادق `undefined === undefined` أحدًا عند غياب متغيّر البيئة.
- كل محاولة فتح — ناجحة أو فاشلة — تُسجَّل في سجلّ المراقبة.
- أُعيدت كتابة قسم `.env.example` ليشرح أن ضبط هذه المتغيّرات في الإنتاج **بلا أثر**، وأن أي فتح دعم مستقبلي يجب أن يكون رمزًا من الخادم محدود الصلاحية ووحيد الاستخدام.

### التحقّق

بنيتُ الإنتاج بأسرار طُعم (canary) وبحثتُ عنها في `dist/`:

```
✅ السرّ غائب تمامًا
grep -r "MASTER_HASH" dist/  →  صفر نتائج
```

وفي حزمة `Settings` تحديدًا: `600000` = 0 · `deriveBits` = 0 · `masterPass` = 0 · `99999` = 0 · `showMasterModal` = 0.

**ملاحظة بصراحة:** نصوص الترجمة (`settings.masterTitle` = "Developer Mode") ما زالت في ملفات اللغات. هي **تسميات خاملة بلا شيفرة تشغّلها** — لا تمنح صلاحية ولا تكشف سرًّا، وحذفها من 11 ملف لغة يزيد مساحة التغيير بلا مكسب أمني. ذكرتُها لتكون على علم لا لأخفيها.

---

## 🥈 المبادرة 2 — التشفير المغلَّف

### العطل، وقد أثبتُّه قبل إصلاحه

المفتاح الذي يحمي البيانات كان يُشتقّ من الـ PIN مباشرة:

```
dataKey = PBKDF2(pin, pinSalt, 600_000)
```

و`handleSavePin` كان **يولّد ملحًا عشوائيًا جديدًا في كل تغيير** ولا يُعيد تشفير شيء. ملح جديد = مفتاح مختلف تمامًا، فتصبح كل البيانات المشفّرة على القرص غير قابلة للفكّ **إلى الأبد**.

اثنا عشر جدولًا: المعاملات، الميزانيات، الأهداف، الديون، الفواتير، الحسابات، الاشتراكات، الاستثمارات، الأقساط، البطاقات، سجلّ المراقبة، سجلّ المحادثات. أي: التاريخ المالي الكامل للمستخدم، يضيع لأنه **اتّبع نصيحة أمنية**.

والأسوأ أن الفقد **صامت**: طبقة الوسيط تبتلع أخطاء فكّ التشفير وتُرجع السجلّ بحقوله ناقصة، فيبدو التطبيق عاملًا والبيانات ذهبت.

### الحلّ: مفتاحان لا مفتاح واحد

```
KEK (مفتاح تغليف) = PBKDF2(pin, pinSalt, 600_000)   ← يُشتقّ، لا يُخزَّن
MDK (مفتاح البيانات) = مفتاح AES-GCM عشوائي 256-bit  ← ثابت لا يتغيّر
المخزَّن: wrappedMDK = AES-GCM(MDK تحت KEK)
```

السجلّات تُشفَّر بالـ **MDK** الذي لا يتغيّر أبدًا. الـ PIN يحمي الغلاف فقط. لذا تغيير الـ PIN = فكّ الغلاف بالمفتاح القديم، وإعادة تغليف **المفتاح نفسه** بالجديد، وكتابة صفّ إعدادات واحد. **لا سجلّ واحد يُقرأ أو يُكتب**، فالعملية لحظية ولا يمكن أن تفشل في منتصفها وتترك نصف قاعدة البيانات بمفتاح ونصفها بآخر.

الوحدة الجديدة `src/core/security/vaultKey.ts` تُفعّل `generateMasterDataKey` و`encryptKey` و`decryptKey` — وكانت موجودة في `crypto.ts` منذ البداية لكن **لم يستدعها أحد قط**.

### ترحيل القدامى بلا لمس سجلّ واحد

بيانات المستخدم الحالي مشفّرة بـ `PBKDF2(pin, pinSalt)`. بدل فكّ وإعادة تشفير كل صفّ — بطيء، وكارثيّ إن انقطع في المنتصف — **نتبنّى ذلك المفتاح نفسه ليكون الـ MDK** ونغلّفه بنفسه:

```
MDK := PBKDF2(pin, pinSalt)      ← المفتاح الذي تستخدمه البيانات أصلًا
wrappedMDK := AES-GCM(MDK تحته)
```

البايتات التي تحمي البيانات **لم تتغيّر**، فكل سجلّ قائم يبقى مقروءًا؛ ما تغيّر هو أن المفتاح صار مُخزَّنًا مُغلَّفًا بدل أن يُعاد اشتقاقه. ومن أوّل تغيير PIN بعد ذلك يصبح الملح حرًّا في الدوران لأن الـ MDK انفصل عنه.

يعمل عند أوّل فتح، يكتب صفًّا واحدًا، ولا يقرأ أي بيانات جدولية، وتكراره لا يفعل شيئًا.

### الرفض الآمن (Fail-Closed)

`unlockVault` تُرجع `null` **ولا تُثبّت أي مفتاح** عند فشل فكّ التغليف، ولا تلجأ أبدًا إلى اشتقاق مفتاح من الـ PIN. ذلك الملاذ هو العطل نفسه: المفتاح المشتقّ لن يطابق النصّ المشفّر، فتفشل القراءات و**تكتب العمليات التالية فوق بيانات سليمة بمفتاح خاطئ**.

- `PinScreen` يبقى مقفلًا إذا فشل فكّ التغليف حتى لو طابقت بصمة الـ PIN.
- `SecurityCard` يُعيد التغليف **قبل** كتابة البصمة الجديدة، فالفشل يترك الـ PIN القديم والبيانات سليمة تمامًا.
- `handleRemovePin` يمسح الغلاف (غلاف يتيم بمفتاح PIN محذوف يتضارب مع الغلاف التالي).
- مسار البصمة الحيوية في `onboarding.ts` صار يفكّ التغليف بدل الاشتقاق.

### التحقّق

16 اختبارًا، وأهمّها **اختبار على مستوى المصدر** يسقط إذا أعاد أحدهم `deriveMasterKey` كمصدر لمفتاح البيانات في أي من مواقع فكّ القفل الثلاثة.

**اختبار الطفرة:** أعدتُ السلوك الأصلي (اشتقاق من الـ PIN) فسقطت **7 اختبارات**. أي أن هذه الاختبارات تمسك العطل فعلًا ولا تكتفي بتغطية الإصلاح.

---

## 🥉 المبادرة 3 — انحراف الفاصلة العائمة

### الحجم الحقيقي، بلا تهويل

قِستُه: **3.3e-11 بعد ألف معاملة**. لا يظهر في أي رقم معروض، ولهذا لم يُبلَّغ عنه كعطل عرض. وهذا بالضبط سبب ترتيبه ثالثًا.

لكنه يكفي تمامًا لكسر **مقارنة**. السيناريو الواقعي الذي وجدتُه:

> هدف بـ 100.03 يُموَّل بسبع دفعات من 14.29. الجمع الساذج يقف عند **100.02999999999997**، فتكون `saved >= target` **خاطئة**، والشاشة تعرض «100.03 من 100.03»، والهدف يرفض أن يكتمل: لا احتفال، لا نقاط، لا حالة إنجاز.

والمثل في سداد آخر قسط من دين، حيث يبقى `remaining` = 2.8e-14 موجبًا فلا يُسجَّل السداد. عطل **غير مرئي وغير قابل لإعادة الإنتاج عند الطلب**، ويبدو للمستخدم أن التطبيق ببساطة يكابر.

### ما فعلتُه

`src/core/money.ts` بتسامح **نصف وحدة صغرى (0.005)**. هذا الرقم مقصود:
- أكبر من الانحراف المرصود بتسعة أسس عشرية تقريبًا ← يبتلعه بأريحية.
- ونصف أصغر فارق يستطيع المستخدم إنشاءه (وحدة صغرى واحدة) ← **لا يمكنه إخفاء نقص حقيقي**.
- وأي مبلغين أقرب من ذلك يُعرضان بنفس الصورة، فاعتبارهما متساويين هو ما يراه المستخدم ويتوقّعه.

طُبِّق عند **نقاط القرار**: اكتمال الهدف (`GoalItem`, `Goals`)، وسداد الدين وشارة حالته (`DebtItem`)، ومعلَم `remaining === 0` في مخطّط السداد (`DebtPayoffPlanner`) الذي كان يعتمد على صفر مضبوط نادرًا ما ينتجه الحساب.

وعند **نقاط التراكم** ليتوقّف الخطأ من منبعه: `applyBalanceDelta` (كان `acc.balance += delta` بلا أي تقريب)، و`goalService.addToGoal`، و`debtService.payDebt`.

**ما لم أفعله:** لم أُحوّل التخزين إلى أعداد صحيحة بالوحدات الصغرى. هو الإصلاح الصحيح بعيد المدى لكنه ترحيل مخطّط عبر 12 جدولًا مشفَّرًا، وأبقيتُه خارج النطاق عمدًا كما نصّت الخطة. ما نُفِّذ هنا يعالج **حيث يعضّ العطل فعلًا**.

### اعتراف: اختباري أمسك خطأً في كودي

أوّل تطبيق لـ `roundMoney` استخدم دفعة `Number.EPSILON` قبل `Math.round(v * 100) / 100`. اختباري أسقطه: ما زال يُرجع **8.16** للقيمة 8.165، لأن الضرب في 100 يُدخل خطأً أكبر من `EPSILON` نفسه (`8.165 * 100 === 816.4999999999999`). استبدلتُه بإزاحة الأُسّ عبر الصورة النصية العشرية، وهي دقيقة. وثّقتُ ذلك في تعليق الدالّة حتى لا يُعيد أحد المحاولة المرفوضة.

كذلك كانت بياناتي الأولى في اختبارين مصطنعة ولا تُنتج الانحراف المقصود؛ استبدلتُها بسيناريو سداد واقعي (100.03 على سبع دفعات من 14.29) يُنتجه فعلًا.

### التحقّق

23 اختبارًا. كل حالة انحراف **تُثبت أوّلًا أن التعبير الساذج يفشل** ثم تُثبت أن الدالّة تنجح — فالاختبارات توثّق العطل لا تكتفي بتغطية الإصلاح.

---

## ما تبقّى مفتوحًا (بصراحة)

هذه لم تكن ضمن المبادرات الثلاث وما زالت قائمة:

| البند | الحالة |
|---|---|
| **C-1** ملفات بناء Android | مُعادة هيكليًا في `ee272e9`، وتبقى `./gradlew assembleDebug` على جهاز فيه Android SDK — لا يوجد `java` في بيئة العمل هذه |
| **H-2** أربعة مفاتيح API بنصّ صريح | مفتوح |
| **M-4** فحص الجذر بمطابقة نصّ User-Agent | مفتوح |
| **M-6** OAuth ضمني بلا PKCE | مفتوح |
| Crashlytics معطّل فعليًا | المرشّح الرابع في الخطة: 10 مواقع تستدعيه بلا `apply plugin` ولا `google-services.json` |
| تخزين النقود كأعداد صحيحة | مؤجَّل عمدًا — ترحيل مخطّط عبر 12 جدولًا مشفَّرًا |
| نصوص ترجمة الباب الخلفي في ملفات اللغات | تسميات خاملة بلا شيفرة تشغّلها |

---

## سجلّ الـ commits

```
0b33183  fix(money): stop float drift from blocking goal completion and debt payoff
ae9c97f  fix(security): envelope encryption — changing the PIN no longer destroys all data
80cc4ec  security(M-3): compile the developer-unlock backdoor out of production builds
35b08a9  docs: bank-grade roadmap (الخطة)
```
