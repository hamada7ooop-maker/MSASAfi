# دليل مشروع مصاريفي (Masarifi) الشامل - v23.0.6 📱💰🏆

مرحباً بك في الوثيقة الفنية والوظيفية الشاملة لمشروع **مصاريفي (Masarifi)**. تم إعداد هذه الوثيقة لتكون مرجعاً متكاملاً يوثق كل تفصيلة في التطبيق، بدءاً من البنية التحتية البرمجية وقاعدة البيانات إلى أدق ميزات واجهة المستخدم والألعاب المدمجة.

---

## 📌 الفهرس العام
1. [نظرة عامة عن المشروع وهويته](#1-نظرة-عامة-عن-المشروع-وهويته)
2. [الهيكل التقني والبنية التحتية (Tech Stack)](#2-الهيكل-التقني-والبنية-التحتية-tech-stack)
3. [بنية مجلدات المشروع (Directory Structure)](#3-بنية-مجلدات-المشروع-directory-structure)
4. [قاعدة البيانات وجداولها بالتفصيل (IndexedDB & Schema)](#4-قاعدة-البيانات-وجداولها-بالتفصيل-indexeddb--schema)
5. [إدارة الحالة (State Management)](#5-إدارة-الحالة-state-management)
6. [نظام الترجمة والتوطين بـ 11 لغة (i18n)](#6-نظام-الترجمة-والتوطين-بـ-11-لغة-i18n)
7. [الطبقة الأمنية وتدابير الحماية (Security & Encryption)](#7-الطبقة-الأمنية-وتدابير-الحماية-security--encryption)
8. [تفاصيل الميزات والصفحات الوظيفية (Core Features)](#8-تفاصيل-الميزات-والصفحات-الوظيفية-core-features)
9. [مركز الألعاب الترفيهي المدمج (Arcade Hub - 24 لعبة)](#9-مركز-الألعاب-الترفيهي-المدمج-arcade-hub---24-لعبة)
10. [نظام الولاء والمكافآت المطور (Loyalty 2.0)](#10-نظام-الولاء-والمكافآت-المطور-loyalty-20)
11. [بيئة التطوير والسكربتات المساعدة (Scripts & Release)](#11-بيئة-التطوير-والسكربتات-المساعدة-scripts--release)

---

## 1. نظرة عامة عن المشروع وهويته
تطبيق **مصاريفي (Masarifi)** هو مدير مالي شخصي ذكي، صُمم بمفهوم **العمل دون اتصال بالإنترنت أولاً (Offline-First)** ويوفر حماية قصوى لخصوصية البيانات المالية الحساسة.
- **الهوية البصرية:** تعتمد واجهات التطبيق بالكامل على **الزجاجي المزدوج الفاخر (Double Glassmorphism)** مع تباينات نيون متوهجة، وخطوط حديثة مخصصة مثل **Tajawal** و **IBM Plex Sans Arabic** لتقديم تجربة مستخدم رائدة ومبهرة بصرياً.
- **الخصوصية والأمان:** لا يتم إرسال أي بيانات مالية خارج الجهاز، وجميع العمليات الحسابية والذكاء الاصطناعي والألعاب تعمل محلياً (Offline 100%).
- **الأمان البرمجي الصارم (Strict Type Safety - Zero `any`):** تطهير المشروع بالكامل بنسبة 100% من استخدامات `any` واعتماد نماذج صارمة لكافة الكيانات وجداول قاعدة البيانات وخدمات التصدير والحسابات.
- **القواعد الثقافية الملتزم بها:** يلتزم التطبيق التزاماً مطلقاً **بعدم استخدام أي رموز أو أيقونات تتعلق بالخنازير** (مثل حصالة الخنزير Piggy Bank)، ويتم استبدالها دائماً بأيقونات مالية إسلامية وعربية متناسقة مثل `monetization_on` و `account_balance` و `wallet`.

---

## 2. الهيكل التقني والبنية التحتية (Tech Stack)
تم تحديث التطبيق مؤخراً وترقيته بالكامل ليعمل بالبنية الحديثة التالية:
- **المحرك الأساسي:** React 19 + TypeScript 5.8 (TS) مع Zero-`any` Type Safety وتغطية TypeScript كاملة 100% لكافة طبقات النواة والخدمات والميزات.
- **أداة البناء والتجميع:** Vite 6.
- **إدارة الحالة:** Zustand 5 مع دعم الحفظ المستمر (Zustand Persist).
- **قاعدة البيانات المحلية:** Dexie.js 4 (مبنية فوق IndexedDB لتقديم أداء استعلامي قوي وسريع).
- **التصميم والتنسيق:** CSS مخصص ومبني بدقة فوق نظام المتغيرات (CSS Variables)، مع استخدام Tailwind CSS v4 للترتيب والمحاذاة السريعة.
- **إطار العمل للأجهزة الذكية:** Capacitor v8 (مما يسمح للتطبيق بالعمل كتطبيق Android أو iOS أصلي بالكامل مع الوصول للميزات البرمجية للهاتف).

---

## 3. بنية مجلدات المشروع (Directory Structure)
المشروع مقسم ومنظم بطريقة تسهل الصيانة والتوسع المستقبلي:
```
TEST/
├── android/                   # مجلد بناء تطبيق الأندرويد الأصلي (Android Project)
├── dist/                      # مخرجات بناء الإنتاج النهائية بعد التجميع
├── docs/                      # توثيقات مخصصة ومحاضر تسليم الجلسات البرمجية
├── protected/                 # النسخ الاحتياطية الموثقة لملفات الترجمة الحساسة
├── public/                    # الأصول الثابتة، الأيقونات، وملف الـ Manifest
├── scripts/                   # السكربتات المساعدة والمؤتمتة (مزامنة الترجمة، الأمان، بناء الحزمة)
├── src/                       # الكود المصدري للتطبيق
│   ├── components/            # المكونات العامة والمشتركة (الهياكل، الأزرار، التنسيق الزجاجي)
│   ├── core/                  # المنطق البرمجي الأساسي (قاعدة البيانات، الأمن، الترجمة، الـ AI)
│   │   ├── db/                # إعدادات قاعدة البيانات والجداول والمستودعات والتهيئة
│   │   ├── security/          # التشفير AES-GCM وطرق الحماية من XSS
│   │   └── services/          # خدمات فحص واستيراد البيانات وحساب الإحصاءات
│   ├── features/              # المجلدات المخصصة للميزات والصفحات (كل صفحة لها مكوناتها ومنطقها)
│   ├── hooks/                 # خطافات React المخصصة لإدارة تهيئة التطبيق والوظائف المشتركة
│   ├── locales/               # ملفات ترجمة اللغات الـ 10 الإضافية (محملة كسولاً عند الطلب Lazy-loaded)
│   ├── store/                 # مخازن Zustand لإدارة حالة التطبيق والإعدادات
│   ├── styles/                # تنسيقات الألوان والسمات المخصصة
│   ├── types/                 # تعريفات TypeScript ونماذج البيانات المشتركة
│   ├── widgets/               # مكونات الشاشة الرئيسية المصغرة (ويدجت)
│   ├── AppRoot.tsx            # التوجيه الأساسي ونقاط الوصول للصفحات (Routes)
│   ├── main.tsx               # نقطة الانطلاق والتهيئة لـ React
│   └── translations.js        # ملف الترجمة الأساسي (العربية والإنجليزية)
├── tests/                     # اختبارات الوحدات والتكامل (Vitest + Jest)
├── package.json               # ملف التبعيات والسكربتات البرمجية
├── tsconfig.json              # إعدادات TypeScript لمطابقة الكود وتتبعه
└── vite.config.ts             # إعدادات أداة التجميع Vite
```

---

## 4. قاعدة البيانات وجداولها بالتفصيل (IndexedDB & Schema)
يستخدم التطبيق **Dexie.js** لإدارة قاعدة البيانات المحلية **MasarifiDB** (النسخة 31 حالياً). تم تنظيم طبقة قاعدة البيانات وفق معمارية معيارية مفصولة المسؤوليات بالكامل:
- **`src/core/db/schema.ts`**: تعريف جداول Dexie وفهارسها وتحديد الحقول المشفرة.
- **`src/core/db/seed/demoData.ts`**: عزل كامل لمجموعة البيانات التجريبية الشاملة (450+ معاملة تغطي 12 شهراً، التقسيم العائلي، المغلفات الرقمية، حسابات الأطفال) ودوال `seedDemoData` و `clearDemoData`.
- **`src/core/db/services/`**:
  - `recurringService.ts`: محرك العمليات المجدولة والمتكررة وتحديث الأرصدة تلقائياً.
  - `goalService.ts`: إدارة إيداعات أهداف الادخار وحركاتها المالية.
  - `debtService.ts`: إدارة سداد وتحصيل الديون بدقة.
  - `billService.ts`: إدارة سداد الفواتير والاشتراكات.
  - `categoryInitService.ts`: تهيئة التصنيفات والحسابات الافتراضية وإصلاح الترتيب وحذف المكررات.
- **`src/core/db/repositories/`**: 16 مستودعاً للكيانات الفردية (`transactions.ts`, `accounts.ts`, `budgets.ts`, `cards.ts`, `assets.ts`... إلخ).
- **أمان الأنواع البرمجية (Strict Type Safety)**: جميع الجداول الـ 19 وكافة دوال CRUD والخدمات والمستودعات والتشفير مؤمّنة بالكامل بنسبة 100% بنماذج TypeScript الصارمة من `src/types/` بدون استخدام أي نوع `any`.

### الجداول والمؤشرات الأساسية:
1. **`transactions`:** لتخزين المعاملات المالية.
   - *المؤشرات:* `id, date, createdAt, category, type, accountId, account, goalId, isDemo, [type+date], [category+type], [accountId+type]`.
2. **`accounts`:** لإدارة المحافظ والحسابات البنكية والنقدية والائتمانية.
   - *المؤشرات:* `id, isDemo`.
3. **`budgets`:** لإدارة الميزانيات الشهرية وتحديد الأسقف الإنفاقية.
   - *المؤشرات:* `id, category, isDemo`.
4. **`goals`:** لأهداف الادخار الشخصية والعائلية.
   - *المؤشرات:* `id, isDemo`.
5. **`debts`:** لتتتبع الديون والقروض (سواء كانت مستحقة لك أو عليك).
   - *المؤشرات:* `id, type, isDemo`.
6. **`bills`:** لتتبع الفواتير الشهرية والسنوية والاشتراكات.
   - *المؤشرات:* `id, dueDate, type, isPaid, isDemo, [type+dueDate]`.
7. **`categories`:** لتخزين تصنيفات المصاريف والدخل.
   - *المؤشرات:* `id, order, isDemo`.
8. **`settings`:** لحفظ إعدادات التطبيق وتفضيلاته.
   - *المؤشرات:* `id, key`.
9. **`auditLog`:** لتتبع تاريخ النشاطات والعمليات داخل التطبيق بدقة لضمان النزاهة وأمن البيانات.
   - *المؤشرات:* `id, timestamp, action`.
10. **`investments`:** لمتابعة الأسهم والسندات والعملات الرقمية والمعادن الثمينة.
    - *المؤشرات:* `id, isDemo`.
11. **`challenges`:** لتخزين تحديات الادخار والإنفاق المختلفة ونسبة التقدم فيها.
    - *المؤشرات:* `id, isDemo`.
12. **`recurringTransactions`:** لتنظيم المعاملات المجدولة تلقائياً (مثل الرواتب والفواتير الدورية).
    - *المؤشرات:* `id, nextDate, isActive, isDemo`.
13. **`chatHistory`:** لتخزين سجل المحادثات المشفّرة والمحلية مع المستشار الذكي.
    - *المؤشرات:* `id, timestamp`.
14. **`notifications`:** لإدارة تنبيهات التطبيق المحلية وسجل الإشعارات.
    - *المؤشرات:* `id, date, timestamp, read`.
15. **`classificationRules`:** لتخزين القواعد المخصصة لتصنيف المعاملات تلقائياً.
    - *المؤشرات:* `id, priority, isActive`.
16. **`trips`:** لإدارة ميزانيات السفر المتعددة.
    - *المؤشرات:* `id, isActive, currency`.
17. **`installments`:** لمتابعة عمليات الشراء بالتقسيط والتزاماتها المستحقة.
    - *المؤشرات:* `id, isDemo, dueDate`.
18. **`assets`:** لتتبع الأصول المادية (العقارات، السيارات، الأجهزة) واحتساب استهلاكها.
    - *المؤشرات:* `id, category, isDemo, purchaseDate`.
19. **`cards`:** لإدارة البطاقات البنكية الائتمانية والمدى.
    - *المؤشرات:* `id, isDemo`.

---

## 5. إدارة الحالة الموحدة (Unified State Architecture)
تم إتمام الترحيل المعماري الشامل بنسبة 100% وإلغاء أي أنظمة حالة قديمة (`state.js` و `EventBus.js` القديمين)، ليصبح التطبيق معتمداً بالكامل على مصدر موحد وحيد للحقيقة التفاعلية باستخدام **Zustand**:

### أ. مخزن التطبيق الرئيسي (`useAppStore`):
- يوجه الحركة الأساسية للمستخدم بين الصفحات المتنقلة (`currentPage`, `previousPage`, `isLocked`).
- يدير الوضع الصامت والتخفي المالي (`incognito`) الذي يخفي الأرصدة تلقائياً بنقرة واحدة.
- ينظم الاختيارات المجمعة (Bulk Selections) للعمليات وتصديرها.
- يحتفظ بالحالة اللحظية لنظام الولاء:
  - `userPoints` (نقاط الولاء الحالية للمستخدم والمزامنة لحظياً مع الألعاب).
  - `loginStreak` (عدد أيام تسجيل الدخول المتتالية).
  - `notifCount` (عدد الإشعارات غير المقروءة).

### ب. مخزن الإعدادات وتفضيلات النظام (`useSettingsStore`):
- يحفظ إعدادات العملة الأساسية، ونظام الأرقام المستخدم (العربي أو الهندي)، واللغة الفعالة مع التزامن الفوري.
- يحفظ الترتيب المخصص للشاشة الرئيسية والميزات السريعة (Quick Access).
- يدير عمليات تشفير قاعدة البيانات والتحقق الحيوي (Biometric Lock).
- **التحديثات المتقدمة:**
  - `envelopes`: مصفوفة لحفظ ومتابعة المغلفات الرقمية وتحديث مبالغها.
  - `childAccounts`: إدارة حسابات الأطفال ودفع مصروفهم الدوري تلقائياً.
  - `streakShields`: حماية الأيام المتتالية عند نسيان التسجيل المالي.

---

## 6. نظام الترجمة والتوطين بـ 11 لغة (i18n)
يقدم التطبيق دعماً كاملاً ومحاذياً للغات في جميع القوائم والواجهات بشكل فوري دون الحاجة للإنترنت.
- **اللغات المدعومة بالكامل (100% Parity):**
  1. العربية (`ar`) - اتجاه RTL.
  2. الإنجليزية (`en`) - اتجاه LTR.
  3. الألمانية (`de`)
  4. الإسبانية (`es`)
  5. الفرنسية (`fr`)
  6. الإيطالية (`it`)
  7. التركية (`tr`)
  8. الأردية (`ur`) - اتجاه RTL.
  9. الفارسية (`fa`) - اتجاه RTL.
  10. الإندونيسية (`id`)
  11. الماليزية (`ms`)
- **حارس الترجمة والتراجم المزامنة:** يتم فحص الترجمات بشكل برمجي عبر سكربت `guardian.mjs` للتأكد من مطابقة جميع مفاتيح اللغات (أكثر من 3027 مفتاح ترجمة لكل لغة) ومنع وجود نصوص فارغة أو مفقودة قبل كل عملية إصدار.

---

## 7. الطبقة الأمنية وتدابير الحماية (Security & Encryption)
تم اتخاذ أقصى التدابير الأمنية لحماية وتشفير البيانات المالية وتلافي أي هجمات خبيثة:
- **تشفير البطاقات المصرفية (AES-GCM):** يتم تشفير تفاصيل الكروت والبطاقات البنكية المخزنة (الأرقام، تاريخ الانتهاء، ورمز الأمان CVV) باستخدام المفاتيح المؤمنة بيومترياً وتوليد نصوص مشفرة تمنع القراءة المباشرة من الذاكرة.
- **حماية Gemini API Key:** يتم استرجاع مفاتيح الذكاء الاصطناعي بطرق آمنة تدعم التحقق الحيوي ويتم تمريرها عبر الروابط الآمنة ورؤوس الشبكة المستقلة (`x-goog-api-key`) بدلاً من query parameters لحمايتها من تسريبات سجلات الشبكة.
- **منع هجمات XSS:** يتم تمرير النصوص والقيم الديناميكية إلى واجهات المستخدم باستخدام دالة أمنية مخصصة تعتمد على **DOMPurify** الصارم مع قائمة بيضاء محكمة لتطهير المحتوى البرمجي.
- **منع لقطات الشاشة وتسجيلها:** لمنع التطبيقات الخبيثة على الهاتف من التجسس، يحتوي التطبيق على ميزات حجب لقطات الشاشة أو تسجيل الفيديو داخل الواجهات المالية الحساسة.

---

## 8. تفاصيل الميزات والصفحات الوظيفية (Core Features)

### 📊 أ. لوحة التحكم والتحليل المتقدم (Dashboard & Analytics)
- **منطقة العمليات العالمية (Global Action Hub):** توجد أعلى الصفحة الرئيسية كمركز قيادة تفاعلي، يوجه المستخدم بذكاء لإضافة معاملة، أو إعداد فاتورة، أو تسديد قرض بمجرد الضغط.
- **أعمدة التقارير المهنية الستة:** تلتزم التقارير والمخرجات بنموذج محاسبي سداسي الأعمدة موحد (التاريخ، النوع، القسم، الوصف، المصاريف، الإيرادات).
- **المجموع التراكمي (Running Balance):** يعرض الرصيد التراكمي الدقيق حركة بحركة في جدول المعاملات بناءً على ترتيب زمني صارم ثنائي المعايير (التاريخ التاريخي + التاريخ الزمني الفعلي للإنشاء `createdAt`) لتلافي فوضى الفرز اليومي المتطابق.
- **الطباعة والتصدير:**
  - تحويل ألوان OKLCH/OKLAB الحديثة ديناميكياً إلى RGB أثناء التصدير لضمان عدم انهيار محرك `html2canvas` على المتصفحات القديمة.
  - دعم التصدير لملفات PDF مائية مخصصة، وجداول Excel و CSV متزامنة حسابياً 100%.

### 🧠 ب. المستشار المالي الافتراضي والمحادثة (AI Financial Advisor)
- **حساب الصحة المالية:** يقيم عادات الصرف والاستثمار شهرياً ويمنح درجة من 100.
- **محلل الإنفاق العاطفي:** يراقب العلاقة بين الحالة النفسية للمستخدم (سعيد، متوتر، حزين) وحجم مصاريفه، ويظهر توصيات ذكية لمنع المشتريات الاندفاعية.
- **مؤقت التبريد (Cooling-off Period):** يقترح تأجيل عمليات الشراء الكبيرة التي تتجاوز حداً معيناً لحمايتك من الشراء غير المحسوب.
- **الذكاء الاصطناعي المتقطع (AI Pagination):** يلتزم المساعد الذكي بقواعد الرد المدمجة لعدم إغراق شاشات الهاتف بنصوص ضخمة (يقسم الإجابات الطويلة على أجزاء ويدعم زر "أكمل").

### ✉️ ج. المغلفات الرقمية الذكية (Digital Envelopes)
- مستوحاة من فكرة الأظرف النقدية التقليدية. يحدد المستخدم سقفاً مالياً معيناً لكل ظرف (مثل ظرف القهوة، ظرف الهدايا) ويقوم التطبيق بتحديث استهلاك الظرف بصرياً مع حركات الصرف المرتبطة.

### 🪙 د. حاسبات الميزات الإسلامية والعربية (Islamic Finance Calculators)
- **حاسبة الزكاة الذكية:** تحتسب نصاب الزكاة الشرعي تلقائياً للذهب والفضة والأسهم والأموال النقدية وفقاً لأسعار السوق اللحظية وتدعم التنبيهات السنوية.
- **ميزانية رمضان والعيدين:** تنظيم مالي تفاعلي لتخطيط مصاريف المناسبات وشراء مستلزمات العيد وزكاة الفطر.

---

## 9. مركز الألعاب الترفيهي المدمج (Arcade Hub - 24 لعبة)
يحتوي التطبيق على واحة ترفيهية متكاملة تعمل بالكامل بدون إنترنت (**100% Offline**). تم تصميمها بنمط النيون والزجاج المزدوج الفاخر لتسلية المستخدمين والمحافظة على تفاعلهم:

### الألعاب الـ 24 المدمجة بالتفصيل:
1. **دمج العملات المالية (Coin Merger):** لعبة تشبه 2048 تعتمد على دمج العملات المعدنية السعودية حتى الوصول لفئة 500 ريال مع تأثيرات Confetti مبهرة.
2. **قفز الديناصور (Endless Dino Run):** سباق نيون ديناصور كلاسيكي باللمس والتفادي بسرعة 60 إطار في الثانية.
3. **الثعبان النيوني (Neon Snake):** الثعبان التقليدي يتحرك على رقعة سيبرانية متوهجة ويدعم اللمس والإيماءات.
4. **تحدي X & O:** اللعبة التقليدية ضد الذكاء الاصطناعي بنظام MiniMax الذكي.
5. **سودوكو الذهني:** ألغاز سودوكو مولدة تلقائياً بثلاث مستويات صعوبة ووضع الملاحظات.
6. **كاسحة الألغام:** الكشف عن الألغام مستخدماً خوارزمية Flood Fill السريعة.
7. **بازل الترتيب المنزلق:** رتب القطع المنزلقة بشعار مصاريفي المتوهج.
8. **تحدي الذاكرة المالي:** مطابقة الكروت ثلاثية الأبعاد للرموز والعملات المالية.
9. **التتريس المطور:** ترتيب وتركيب القطع المتساقطة مع تزايد مستمر في السرعة.
10. **تدمير الكتل:** تحريك مضرب زجاجي لتدمير كتل النيون بالكرات.
11. **غزاة الفضاء:** صد غزو الكائنات الفضائية مع حواجز دفاعية متحللة تدريجياً.
12. **الكويكبات:** قيادة مركبة فضائية وتفجير الكويكبات الهندسية المتدحرجة.
13. **باك مان (Pac-Man) 🟡:** التهام النقاط الذهبية وتفجنب الأشباح الأربعة الأذكياء.
14. **عبور الضفدع (Frogger) 🐸:** عبور الطرق السريعة المزدحمة وتخطي جذوع النهر.
15. **التنس الكلاسيكي (Pong) 🏓:** مباراة تنس الطاولة الكلاسيكية ضد الذكاء الاصطناعي.
16. **الطائر الرفراف (Flappy Bird) 🐦:** التحليق بالطائر عبر الأنابيب الزجاجية المتوهجة بفيزياء الجاذبية.
17. **تخمين الكلمة اليومية (Wordle) 🔠:** تخمين الكلمة السرية المكونة من 5 أحرف في 6 محاولات مع الكلمات المالية المخصصة.
18. **كتل البناء المتأرجحة (Tower Bloxx) 🏗️:** إسقاط الكتل بدقة لبناء أطول ناطحة سحاب.
19. **لغز الجواهر المتطابقة (Match-3) 🟪:** طابق 3 جواهر متجاورة أو أكثر لتفجيرها قبل نفاد الحركات. (مصلحة من الارتجاف).
20. **الدراجات النيونية (Light Riders) 🛞:** قيادة دراجة نيونية وترك خلفك ذيلاً مضيئاً لإجبار الخصوم على الاصطدام به.
21. **صياد الذهب والكنوز (Gold Miner) ⛏️:** تأرجح الخطاف لسحب الذهب وتفادي الصخور والحجارة الثقيلة.
22. **لغز الكتل الشبكي (Block Puzzle) 🔲:** سحب وإسقاط الكتل الهندسية لإكمال صفوف كاملة في شبكة 8×8 ومسحها.
23. **متاهة الجاذبية والكرة (Gravity Maze) 🌀:** توجيه الكرة عبر التحكم في الجاذبية للوصول للهدف وتفادي الثقوب الحمراء.
24. **الداما السيبرانية (Cyber Checkers) 🏁:** لعبة الداما الكلاسيكية والتكتيكية على رقعة شطرنج نيونية ضد ذكاء اصطناعي حسابي.

---

## 10. نظام الولاء والمكافآت المطور (Loyalty 2.0)
يهدف هذا النظام لتشجيع المستخدم على الانضباط المالي والادخار من خلال آليات تلعيب متكاملة:
- **مكافآت الألعاب اليومية:** يحصل المستخدم على **15 نقطة ولاء تشجيعية** (أو 5 نقاط للمستوى السهل، 30 نقطة للمستوى الصعب) عند كسر الرقم القياسي في أي لعبة، بحد أقصى مرة واحدة يومياً لكل لعبة لمنع التكرار اللانهائي وتأمين النظام.
- **مكافأة الإنجازات (Milestones):** يحصل المستخدم على نقاط كبيرة عند تحقيق إنجاز مالي حقيقي، مثل:
  - إضافة البطاقة المصرفية الأولى (`FIRST_CARD`): 100 نقطة.
  - إنجاز تحدي الادخار الأول: 150 نقطة.
- **متجر المكافآت (Loyalty Shop):** يمكن استبدال نقاط الولاء المحرزة لشراء مزايا رقمية داخل التطبيق، مثل:
  - تفعيل الوضع الرياضي الفاخر.
  - تفعيل النسخة المدفوعة من المستشار الذكي (`AI Pro`).
  - شراء دروع الحماية Streak Shields.

---

## 11. بيئة التطوير والسكربتات المساعدة (Scripts & Release)

### هرم الاختبارات الشامل (Testing Pyramid Architecture - 138 Tests):
يعتمد المشروع على هرم اختبارات احترافي ثلاثي المستويات يضمن دقة وموثوقية كافة الحسابات والواجهات:
1. **اختبارات الوحدة (Unit Tests):** تغطي منطق الأعمال المعقد في طبقة الخدمات المالية (`DebtService`, `GoalService`, `RecurringService`)، وتحليل الرسائل البنكية والفواتير (`SMSParser`, `OCRParser`)، ومحرك التصنيف بالذكاء الاصطناعي واكتشاف العمليات الشاذة (`ai.ts`)، وتجهيز بيانات التصدير والمجموع التراكمي (`ExportService`).
2. **اختبارات المكونات (Component Tests):** مبنية فوق `@testing-library/react` لاختبار سلوك مكونات الواجهة مثل `TransactionItem`, `GoalItem`, `DebtItem`.
3. **اختبارات التكامل (Integration Tests):** تغطي دورات الحياة المالية الحقيقية من البداية للنهاية (Add Transaction ➔ Dexie DB ➔ Balance Adjustment ➔ Notification/Audit).

---
> 📝 **ملاحظة فنية للمطورين:** عند إضافة أي نص أو تعديله في الواجهات، يرجى تشغيل `node scripts/guardian.mjs validate` قبل تقديم الإصدار للتأكد من المحاذاة التامة للترجمات وخلوها من الأخطاء الهيكلية.
