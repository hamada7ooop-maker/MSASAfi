# التوجيه 19 — ثورة الـ UI/UX الشاملة: مقترح وخارطة طريق
**حالة المستند:** مقترح على طاولة المالك للاعتماد · كتبه المدقق بعد مسح البنية البصرية الحالية · 2026-09-15

---

## 0. الفلسفة الحاكمة (قبل كل شيء)

تطبيقنا **مالي** — والجمال هنا ليس زينة بل **وضوح وثقة**: المستخدم يقرأ رصيده في أقل من ثانية، ويثق بأن الرقم الذي يراه صادق. كل قرار بصري في هذا المقترح يُقاس بثلاثة أسئلة:

1. هل يجعل **الأرقام** أوضح وأسرع قراءة؟
2. هل يزيد **الثقة** (الملمس المتقن يُقرأ لاواعياً كموثوقية مالية)؟
3. هل يحافظ على **60 FPS** على الأجهزة المتوسطة؟

ثلاث قواعد حديدية لا تُكسر في أي دفعة:
- **الاختبارات الـ1,112 والسقاطة (63.9/58.8/48.0) خط أحمر** — كل دفعة تسلّم `ci:check` exit 0 كاملة.
- **توصيف قبل الهدم**: أي مكوّن يُعاد بناؤه يُوثَّق سلوكه باختبارات *قبل* لمسه، ثم يُعاد بناؤه فوقها.
- **دفعات صغيرة قابلة للشحن** (≤ ~10 ملفات لكل التزام): لا Big-Bang أبداً — كل دفعة مستقلة القيمة وقابلة للتراجع.

---

## 1. نقطة الانطلاق: ما لدينا فعلاً (المسح)

المسح كشف أن الأساس **أقوى مما يُتوقع** — والثورة ستكون *تطويراً موجهاً* لا هدمها:

| الأصل الموجود | الحالة | القرار |
|---|---|---|
| Tailwind v4 CSS-first (`@theme` في `src/index.css`) | ✅ نظام رموز دلالي كامل بنمط Material 3 («Stitch — Financial Architect»): primary كحلي `#002b59`، secondary أخضر، tertiary أحمر، سلّم surfaces كامل | **نبني فوقه** — نعيد ضبط القيم لا الأسماء |
| وضع ليلي بـ`.dark` + `@custom-variant dark` + `darkPalette` | ✅ لوحة `dim` افتراضية + 6 لوحات نهارية (`data-light-palette`) عبر `ThemeManager.tsx` | نضيف لوحة **Obsidian** كلوحة ليلية جديدة خارقة، ونجعلها الافتراضية |
| `@capacitor/haptics` | ✅ مثبتة لكنها محصورة في `src/core/ux.ts` القديم (DOM-based) | طبقة **مفردات لمسية** جديدة موحدة للـReact |
| `AnimatedNumber` | ✅ عدّاد rAF يحترم `prefers-reduced-motion` (ميزة الزكاة فقط) | يُعمَّم كبدائية `<NumberFlow/>` لكل الأرصدة |
| الخطوط | ⚠️ **تشرذم**: 4 عائلات (Tajawal/Inter/Manrope/IBM Plex Sans Arabic) بأولويات متناقضة بين `index.css` و`index.html` | توحيد عائلة عربية متغيرة واحدة + خط أرقام tabular |
| الحواف والظلال | ⚠️ مبعثرة: `rounded-3xl` و`rounded-[2.5rem]` و`rounded-2xl` في نفس الشاشة | سلّم موحد من 5 درجات |
| المكونات الثقيلة | ⚠️ BankCardsManager 553 سطراً · QuickAddModal 490 · ClassicDashboard 446 | تفكيك لقطع قابلة للاختبار (يرفع التغطية تلقائياً) |
| عنقود 0% تغطية | ⚠️ 13+ مكوّناً بصرياً بلا اختبارات | التفكيك + التوصيف القبلي يحلها بنيوياً |

---

## 2. المحور الأول — نظام التصميم والرموز (Design Tokens)

### 2.1 الوضع الليلي الفخم: Obsidian
الأردواز العميق المقترح `#0B0F17` يصبح قاعدة لوحة ليلية جديدة كاملة، **بسلّم طبقات مرتفع** (سر العمق الحقيقي ليس اللون بل تدرّج الطبقات):

```
--color-obsidian-950: #070A10   (قاع الشاشة — أعمق من الأساس للتباين الحسي)
--color-obsidian-900: #0B0F17   (الأساس — طلب المالك)
--color-obsidian-800: #111826   (بطاقة المستوى الأول)
--color-obsidian-700: #161F2E   (بطاقة مرفوعة / ورقة سفلية)
--color-obsidian-600: #1D2839   (عناصر تفاعلية مضغوطة)
--color-obsidian-500: #243146   (حدود outline-variant)
```
- **القاعدة الذهبية لليلة**: الظلال السوداء تختفي على الأسود — في Obsidian يُستبدل «الارتفاع» بـ **حدود 1px شفافة + وهج محيطي**، لا `box-shadow` ثقيلة.
- الألوان الدلالية المالية (أهم قرار في التطبيق): **دخل** أخضر زمردي مضيء `#34D399` و**مصروف** مرجاني `#FB7185` — زوج قابل للتمييز لعمى الألوان الأحمر-الأخضر (اختبار deuteranopia) وبتباين ≥ 4.5:1 على Obsidian (WCAG AA).
- الهوية: الكحلي الملكي `#002b59` يبقى brand، لكن في الليل يتحول لنسخته المضيئة `--color-primary-night: #7CB0FF` (الكحلي الغامق يموت على الأسود).

### 2.2 الوضع النهاري النظيف
- قاعدة بيضاء دافئة `#FAFBFC` بأسطح بيضاء نقية وحدود `#E5E7EB` وظلال ناعمة منخفضة الانتشار (لا تعقيم بارد — دفء طفيف يحفظ الهوية).
- اللوحات النهارية الست القائمة (soft/cool/sepia...) تبقى كميزة شخصية؛ Obsidian تصير لون `dim` المطوّر.

### 2.3 الزجاجية والوهج — بتصريح أداء صارم
- `GlassPanel` (بدائية واحدة): `backdrop-blur(16px)` + خلفية `rgba(255,255,255,0.06)` + حد `rgba(255,255,255,0.09)` — **للطبقات العائمة فقط** (شرائط ثابتة، أوراق سفلية، شارات فوق الرسوم). **ممنوع** على القوائم الطويلة أو البطاقات المتداخلة — الـblur أغلى عملية تركيب في المتصفح، وميزانيته: **≤ 3 أسطح زجاجية مرئية في أي شاشة**.
- `AmbientGlow`: توهج radial-gradient ناعم خلف بطاقة الرصيد الرئيسية فقط (زمردي عند صافي موجب، مرجاني عند سالب، بشفافية 10-14%) — يعطي «الحياة» بلا كلفة رسم (طبقة واحدة ثابتة).

### 2.4 توحيد الحواف والظلال (Radii & Elevation)
سلّم نصف قطر واحد من 5 درجات يستبدل كل القيم العشوائية:
`--radius-sm: 12px` (شرائح) · `--radius-md: 20px` (بطاقات فرعية) · `--radius-lg: 28px` (بطاقات رئيسية) · `--radius-xl: 36px` (أوراق سفلية) · `--radius-full` (أزرار دائرية).
سلّم ارتفاع من 4 مستويات (`--elevation-1..4`): نهاراً ظلال رمادية متدرجة؛ ليلاً حدود + وهج (فوق).

### 2.5 تايبوجرافيا الأرقام المالية (الجوهرة)
- **كل رقم مالي في التطبيق يُعرض عبر بدائية واحدة `<NumberText/>`** تفرض:
  - `font-variant-numeric: tabular-nums` — **ثبات العرض**: الأرقام لا «ترقص» أفقياً عند التحديث الحي (التحديد الأدق لجوهر «Linear-grade»).
  - تجميع الآلاف موحداً حسب اللغة (`Intl.NumberFormat` بالفعل موجود في `useFormat`).
  - `letter-spacing: -0.01em` للمبالغ الكبيرة (إحكام بصري).
- **توحيد الخطوط**: عائلة عربية متغيرة واحدة للنصوص (**IBM Plex Sans Arabic Variable** — موجودة أصلاً في المشروع، تدعم العربية واللاتينية معاً بملف واحد، أوزان 100-900) + خط أرقام أحادي العرض للجداول. حذف Tajawal/Manrope/Inter المتشرذمة = **حزمة خطوط أصغر وتناسق كامل**.
- سلّم أحجام محكوم: 12 / 14 / 16 / 20 / 28 / 40 + قاعدة: المبلغ الرئيسي للشاشة دائماً 28-40 بوزن 800.

---

## 3. المحور الثاني — أولويات الشاشات وإعادة البناء

الترتيب بمقياس (تكرار الاستخدام × الأثر العاطفي × المخاطرة):

| الدفعة | الشاشة | لماذا أولاً | الفكرة التصميمية المركزية |
|---|---|---|---|
| **ب (الغنيمة الأولى)** | **لوحة التحكم ClassicDashboard** | أول شاشة يراها المستخدم كل يوم | بطاقة رصيد زجاجية واحدة مهيبة مع AmbientGlow دلالي + NumberFlow + نبضات السوق كسطر شريطي أنيق بدل بطاقات متكدسة |
| **ج** | **الإضافة السريعة (QuickAddModal/GlobalActionModal)** | أهم *تفاعل* مالي — هنا تُبنى ثقة اللمس | ورقة سفلية زنبركية بمقبض سحب، لوحة أرقام كبيرة بإبهام واحد، haptic عند كل تأكيد |
| **د** | **البطاقات والمحافظ (BankCardsManager 553 سطراً)** | أكبر مكوّن ثقيل + تجربة «محفظة» هي هوية Revolut | تفكيك لـ6 قطع (Deck البطاقات، بطاقة مفردة، مدير التفاصيل…) + عمق 3D خفيف للبطاقات |
| **هـ** | **الرسوم البيانية والتقارير (chart.js)** | حيث تُقرأ القصة المالية | شبكة رسم موحدة الألوان الدلالية، تدرجات ناعمة، تلميحات زجاجية، خطوط شبكة باهتة |
| **و** | الشاشات الثانوية بالجملة | إتمام الاتساق | تطبيق البدائيات آلياً — أرخص دفعة لأن الأساس جاهز |

**منهجية التفكيك** (تحل عقدتين بعقدة واحدة): كل مكوّن >400 سطر يُفكك لقطع `≤120 سطراً` في مجلده `components/` الخاص، وكل قطعة **تُختبر منفردة** — وهذا يرفع تلقائياً عنقود الـ0% تغطية (13+ مكوّناً) الذي يعجز الاختبار عنه اليوم بسبب ضخامة الملفات. الجمال والتغطية وجهان لعملية تفكيك واحدة.

---

## 4. المحور الثالث — الحركية والتفاعل اللمسي

### 4.1 نظام حركة توكيني (Motion Tokens)
ملف `src/components/motion/tokens.ts` واحد يعرّف **4 ينبوعات قياسية** تُستخدم حصرياً (الاتساق يصنع الإحساس الفاخر — العشوائية تكسره):
```
spring.snappy:  { stiffness: 500, damping: 30, mass: 0.9 }   // أزرار، تبديل
spring.smooth:  { stiffness: 260, damping: 28, mass: 1.0 }   // أوراق سفلية، تنقلات
spring.gentle:  { stiffness: 170, damping: 24, mass: 1.1 }   // ظهور بطاقات
spring.playful: { stiffness: 700, damping: 22, mass: 0.6 }   // ردود فعل صغيرة (نجاح)
```
**القرار الهندسي**: بلا framer-motion — الحركات تُنفذ بـCSS transitions بمنحنيات spring-approximation + Web Animations API للحالات البرمجية (عدّادات، سحب). حزمة أصغر، وتكامل تام مع القاعدة الحديدية: **transform/opacity فقط**.

### 4.2 NumberFlow — عدّاد الأرصدة الانسيابي
تعميم `AnimatedNumber` الحالي (الذي يحترم reduced-motion — نحافظ على هذا الأدب) إلى `<NumberFlow/>` بتحسينات: تنعيم أسي بمدة متكيفة مع المسافة (قفزات صغيرة 200ms، كبيرة 800ms)، تثبيت العرض بtabular-nums أثناء العد (لا رقص)، ووميض لوني خاطف (زمردي/مرجاني) عند اتجاه التغير.

### 4.3 المفردات اللمسية (Haptic Vocabulary) — الأهم للإحساس «الفيزيائي»
طبقة واحدة `src/core/haptics.ts` — **لا استدعاء مباشر لـ@capacitor/haptics من أي مكان آخر**:
```
touch.light()      // لمس أزرار عادية (ImpactStyle.Light)
touch.select()     // اختيار شريحة/تبديل (Medium)
touch.confirm()    // تأكيد معاملة مالية (Medium + نمط نجاح خفيف)
touch.destruct()   // حذف/مسح (Heavy — الوزن يوصل الخطورة)
touch.error()      // رفض إدخال (Notification.Error)
touch.triumph()    // تحقيق هدف/تجاوز نصاب (نمط مزدوج مميز)
```
- تحترم مفتاح تعطيل جديد في الإعدادات (`hapticsEnabled`)، وتُلغى تلقائياً مع `prefers-reduced-motion`، وتعمل فقط على المنصة الأصلية (web = no-op).
- **إيماءات اليد السفلية**: قانون الإبهام — كل الإجراءات المتكررة في النصف السفلي من الشاشة؛ الأوراق السفلية قابلة للسحب للإغلاق مع مقاومة مطاطية (rubber-band) عند التجاوز، وسرعة السحب تحدد ما إذا كان «تصفح» أم «إغلاق».

---

## 5. المحور الرابع — الأداء والاستقرار التام

### 5.1 قواعد 60 FPS (تُفرض بمراجعة كل PR)
1. الحركات على **transform/opacity فقط** — أي `transition` على width/top/left/margin يُرفض.
2. ميزانية الـblur: ≤ 3 أسطح زجاجية مرئية لكل شاشة (فوق).
3. `content-visibility: auto` للشاشات الطويلة (القوائم) + `contain: layout paint` للبطاقات.
4. الرسوم البيانية (chart.js): تحميل مؤجل عند الوصول للشاشة، وتجميد الرسم أثناء السحب.
5. `will-change` بوصفة طبية فقط (طبقة تُنشأ وتُزال) — ليست دهاناً عاماً.

### 5.2 حماية «الصفر انحدار» (هنا خبرة هذا المشروع تتكلم)
- **قبل** كل مكوّن يُعاد بناؤه: اختبارات توصيف لسلوكه الحالي (النصوص المعروضة، الاستدعاءات، الحالات) — تُكتب وهي خضراء على القديم.
- **بعد**: نفس الاختبارات خضراء على الجديد (الدلالة محفوظة) + اختبارات بدائيات جديدة.
- كل دفعة: `tsc` 0 · `ci:check` exit 0 (1112+ اختباراً وسقاطة التغطية ترتفع ولا تهبط) · guardian نظيف.
- **بوابة بصرية من المالك**: بعد كل دفعة، بناء على جهازك (خط `@abc`) وفحص بصري فعلي قبل اعتماد الدفعة التالية — عين المالك هي بوابة CI الأخيرة.
- ميزانية حجم: حزمة الويب النهائية ≤ الحالية +5% (حذف 3 خطوط يعوّض الإضافات غالباً).

### 5.3 قابلية الوصول (غير قابل للتفاوض)
تباين WCAG AA على كل النصوص المالية · احترام `prefers-reduced-motion` في كل حركة (سابقة قائمة في AnimatedNumber نعممها) · زوج ألوان دخل/مصروف آمن لعمى الألوان · أهداف لمس ≥ 44px.

---

## 6. خارطة الطريق التنفيذية (6 دفعات)

| # | الدفعة | المحتوى | بوابة الخروج |
|---|---|---|---|
| **0** | **التمكين** (بلا تغيير بصري تقريباً) | رموز Obsidian في `@theme` + سلّم الحواف/الارتفاع + توكينات الحركة + `NumberText`/`GlassPanel`/`haptics.ts` + توحيد الخط — كلها ببدائيات جديدة **مختبَرة** (البصريات القديمة لم تُلمس بعد) | اختبارات البدائيات + ci:check exit 0 |
| **1** | **اللوحة الرئيسية** | بطاقة الرصيد الزجاجية + AmbientGlow + NumberFlow + تفكيك ClassicDashboard إلى 5-6 قطع مختبرة | توصيف قبلي/بعدي + مراجعة بصرية من المالك |
| **2** | **الإضافة السريعة** | ورقة سفلية زنبركية + لوحة إبهام + المفردات اللمسية في التفاعل المالي | مثل أعلاه |
| **3** | **البطاقات والمحافظ** | تفكيك الـ553 سطراً + عمق البطاقات + سلسلة السحب | مثل أعلاه |
| **4** | **الرسوم والتقارير** | نظام رسم موحد الدلالة + تلميحات زجاجية | مثل أعلاه |
| **5** | **الاتساق الشامل + الصقل** | الشاشات الثانوية + مسح الحواف العشوائية المتبقية + تقرير ختامي | إغلاق التوجيه بإصدار **v23.3.0** |

كل دفعة = 1-3 التزامات قابلة للدفع والتراجع، ويوم عمل تدقيقي واحد تقريباً.

---

## 7. ما أحتاجه من المالك للاطلاق

1. **اعتماد الاتجاه** (أو تعديله) — وخاصة: اعتماد لوحة Obsidian كافتراضية ليلية، وزوج الدخل/المصروف الزمردي/المرجاني.
2. **قرار الهوية**: إبقاء الكحلي `#002b59` brandاً (مع نسخته الليلية المضيئة) — أم ميل لذهبي ملكي؟ (أوصي: الكحلي — الهوية المكتسبة أغلى من الإعجاب اللحظي).
3. **قرار الخط**: IBM Plex Sans Arabic Variable كعائلة وحيدة (توصيتي) — أم إبقاء تعددية حالية؟
4. **الالتزام ببوابة الفحص البصري**: بناء سريع بعد كل دفعة على جهازك قبل اعتماد التالية.
5. بعد الدفعة 0 والدفعة 1 تحديداً: ملاحظاتك الجمالية الصريحة — الانحراف التصميمي يصحح وهو رخيص.

---

*«الرفاهية الحقيقية في تطبيق مالي ليست الذهب والزخرفة — بل الإحساس بأن كل شيء في مكانه بالضبط، ويعمل بسرعة الضوء، ولا يكذب عليك أبداً.»*
