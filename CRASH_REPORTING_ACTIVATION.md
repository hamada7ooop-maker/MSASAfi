# 🔭 تفعيل رصد الأعطال — دليل الخطوات المتبقية

**الحالة:** مُجهَّز بالكامل ومُعطَّل عمداً · **الالتزام:** `6cfb85a`

كل الشيفرة جاهزة: التهيئة، ومسار الإرسال، وطبقة التعقيم، والاختبارات.
ما تبقّى **خطوتان لا أستطيع تنفيذهما من هنا** لأنهما تتطلّبان حسابك على Firebase.

---

## لماذا مُعطَّل الآن؟

لأن تفعيله يتطلّب `apply plugin` في Gradle، و**إضافة هذا السطر بدون
`google-services.json` تكسر البناء فوراً**. وأنت تشحن v23.0.9 موقّعة من هذا
الفرع، فلن أخاطر ببنائك.

`android/app/build.gradle` **لم يُمَس** — تحقّقتُ من ذلك بعد التعديل.

---

## ما كان معطوباً فعلياً (للسياق)

| الشرط | قبل | بعد |
|---|---|---|
| `classpath` في `android/build.gradle` | ✅ موجود | ✅ موجود |
| `apply plugin` في `android/app/build.gradle` | ❌ غائب | ❌ **غائب عمداً** — خطوتك |
| `google-services.json` | ❌ غائب | ❌ **غائب** — خطوتك |
| `initCrashlytics()` مُستدعاة | ❌ **أبداً** | ✅ في `useAppInitialization` |
| طبقة تعقيم | ❌ لا شيء | ✅ `crashSanitizer.ts` |
| راية تحكّم | ❌ لا شيء | ✅ `VITE_CRASH_REPORTING` |

النتيجة قبل الإصلاح: **205 مسار خطأ** (171 `silentFail` + 34 استدعاء مباشر)
تمرّ عبر `recordException` وتذهب إلى العدم، بينما التعليق في `errors.ts` يقول
صراحةً *"without silent swallows"*. عطل صامت تظنّه مُراقَباً **أخطر** من عطل
تعلم أنه غير مُراقَب.

---

## الخطوات الأربع بالترتيب

### 1. ملف التهيئة (أنت)

من وحدة تحكّم Firebase: أنشئ تطبيق Android بمعرّف الحزمة `com.masarifi.app`،
ثم نزّل `google-services.json` وضعه في:

```
android/app/google-services.json
```

> `.gitignore` يستبعده (السطران 19-20) — وهذا صحيح، لا تُودعه في المستودع.

### 2. سطرا Gradle (أنت أو أنا)

في `android/app/build.gradle`، بعد `apply plugin: 'com.android.application'`:

```gradle
apply plugin: 'com.google.gms.google-services'
apply plugin: 'com.google.firebase.crashlytics'
```

مدخلات `classpath` موجودة أصلاً في `android/build.gradle` (الأسطر 10-12).

### 3. الراية (عند بناء الإصدار)

```bash
VITE_CRASH_REPORTING=true npm run build
```

بدونها لا يُرسَل شيء. وضبطها وحدها على بناء بلا تهيئة Firebase **لا يفعل شيئاً**
ولا يتسبّب بخطأ.

### 4. التحقّق

ابنِ نسخة إصدار، افتعل عطلاً تجريبياً، وتأكّد من وصوله إلى لوحة Firebase خلال
دقائق. **لا تعتبر البند مُغلقاً قبل رؤية تقرير واصل فعلاً** — هذا بالضبط الخطأ
الذي أوقع المشروع في «عمى الإنتاج» أول مرة: وجود الشيفرة لا يعني وصول التقارير.

---

## ماذا يُرسَل وماذا لا يُرسَل

`src/core/crashSanitizer.ts` ينقّي **كل** حمولة قبل الإرسال.

**يُحذف:**

| الفئة | أمثلة |
|---|---|
| بيانات اعتماد | `?apiKey=`، `access_token`، `Bearer`، JWT، `AIza…`، `sk-…`، هيكس ≥32، `pinHash`، `wrappedMDK` |
| هوية شخصية | البريد، IBAN، أرقام البطاقات، الهواتف (+966…)، مسارات الملفات |
| مبالغ مالية | أي رقم بفاصلة عشرية، وأي عدد صحيح من 4 خانات فأكثر، والمبالغ المنسّقة |

**يبقى:** نوع الخطأ (`TypeError`)، ووسم السياق (`[DB]`، `[Cloud]`)، ومسار
الكود، وأسماء الملفات وأرقام الأسطر. أي: ما يجعل التقرير **قابلاً للتصرّف**.

**الحدود:** الرسالة 500 حرف، والمكدّس 20 إطاراً.

### المبدأ الحاكم

التعقيم **يبالغ في الحذف عمداً**. فقدان رقم من تقرير يكلّف المهندس بعض السياق،
وتسريب رقم يكلّف المستخدم خصوصيته. المفاضلة ليست متقاربة، لذا يُحسم الشكّ دائماً
لصالح الحذف.

**استثناء مقصود:** سجلّ التطوير (`npm run dev`) **لا يُعقَّم**. التعقيم يحمي
المستخدم من طرف ثالث، ولا معنى لأن يحجب عن المطوّر بيانات هي أمامه على جهازه.

---

## الاختبارات التي تحرس هذا

| الملف | العدد | يغطّي |
|---|---|---|
| `tests/unit/crashSanitizer.test.ts` | 28 | بيانات الاعتماد، الهوية، المبالغ، ما يجب أن **يبقى**، الحدود، المدخلات الفارغة |
| `tests/unit/crashlytics.test.ts` | 12 | الراية، فشل التهيئة، فشل الإرسال، واختبار شامل أن لا مبلغ ولا بريد ولا مفتاح يصل للناقل |
| `tests/unit/logger.test.ts` | 3 | التوجيه عبر `logger`، وعدم الإرسال والراية مطفأة |

---

## ملاحظة صريحة

الاختبارات الثلاثة القديمة في `crashlytics.test.ts` **سقطت** عند هذا التغيير،
وكان سقوطها صحيحاً: كانت تُرسّخ العقد القديم («أصلي ⇒ يُرسِل»)، وقد أضفتُ شرط
الراية عمداً. أعدتُ كتابتها للعقد الجديد بدل تعطيلها أو التحايل عليها.
