# 🔍 مراجعة تدقيقية للدليل المرجعي الشامل

**الملف المراجَع:** `masarifi_reference_book.md` (2602 سطر، 158 ك.ب، 52 فصلاً)
**تاريخ المراجعة:** 13 سبتمبر 2025
**المنهج:** مقابلة كل ادعاء رقمي أو بنيوي بالكود الفعلي في المستودع — لا اعتماد على أي وصف داخل الوثيقة نفسها.

---

## 0. الحكم العام

**الوثيقة ممتازة من حيث الشمول والتنظيم، ودقيقة في أغلب تفاصيلها الدقيقة، لكنها ليست "صحيحة تماماً بدون أخطاء".**

وجدتُ **23 خطأً مؤكداً**، منها **5 أخطاء جوهرية** تجعل الوثيقة مُضلِّلة لمن يعتمد عليها كمرجع (خصوصاً فصل قاعدة البيانات وفصل التوجيه).

المفاجئ أن الأرقام الصعبة التي توقعتُ أن تكون خاطئة جاءت **مضبوطة بدقة لافتة**: عدد أسطر كل لعبة من الـ 24 (بفارق سطر واحد فقط — وهو فارق `wc -l` المعتاد)، وعدد ودجات الشاشة الرئيسية (27)، وعدد ملفات الأنواع (25)، وعدد النصائح (189). هذا يدل على أن كاتب الوثيقة فحص الكود فعلاً ولم يؤلّف.

لكن المشكلة المنهجية الأخطر ليست في الأرقام:

> **الوثيقة تصف النوايا أحياناً لا الواقع، ولا تذكر أي عيب أو قيد أو مشكلة معروفة — ولا حتى أن التطبيق كان غير قابل للبناء على أندرويد إطلاقاً.**

مرجع بلا قسم "القيود والمشاكل المعروفة" يمنح القارئ ثقة زائفة. وهذا بالضبط ما حدث في التدقيق السابق: التوثيق ادّعى حماية من لقطات الشاشة بينما `FLAG_SECURE` لم يكن مُفعّلاً أصلاً.

---

## 1. الأخطاء الجوهرية (يجب تصحيحها)

### 🔴 خطأ ١ — فصل قاعدة البيانات: ٦ جداول من أصل ٢٠ غير موجودة

الفصل 4 يسرد "20 جدولاً"، لكن الجداول التالية **غير موجودة في `src/core/db/schema.ts` إطلاقاً**:

| الجدول المذكور في الوثيقة | الحقيقة |
|---|---|
| `categoryRules` (جدول 8) | ❌ الاسم الفعلي `classificationRules` |
| `recurring` (جدول 14) | ❌ الاسم الفعلي `recurringTransactions` |
| `audit` (جدول 18) | ❌ الاسم الفعلي `auditLog` |
| `familyMembers` (جدول 16) | ❌ غير موجود نهائياً |
| `childAccounts` (جدول 17) | ❌ غير موجود نهائياً |
| `categoryLearning` (جدول 20) | ❌ غير موجود نهائياً |

**وفي المقابل، الوثيقة أغفلت ٤ جداول موجودة فعلاً:** `installments` و `chatHistory` و `settings` و `classificationRules`.

العدد الفعلي للجداول في `this.version(31).stores({...})` هو **20 جدولاً** — فالرقم الإجمالي صحيح بالمصادفة، لكن **القائمة نفسها خاطئة في 6 مواضع**. القائمة الصحيحة:

```
transactions, budgets, goals, debts, bills, accounts, subscriptions,
investments, challenges, categories, recurringTransactions, settings,
auditLog, notifications, chatHistory, classificationRules, trips,
installments, assets, cards
```

> **لماذا هذا خطير:** مبرمج يقرأ الوثيقة ويكتب `db.audit.toArray()` سيحصل على خطأ وقت التشغيل. والأسوأ: ميزتا "العائلة" و"حسابات الأطفال" موصوفتان في الفصل 22 بتفصيل، لكن لا جداول لهما — إما أنهما تُخزَّنان في مكان آخر (`familyStore.ts`) أو أن الوصف يسبق التنفيذ.

---

### 🔴 خطأ ٢ — خريطة الحقول المشفّرة خاطئة وناقصة (الأخطر أمنياً)

الفصل 4.3 يعرض جدولاً لـ `ENCRYPTED_FIELDS` **يخالف الكود الفعلي مخالفة كبيرة**.

الوثيقة تذكر **6 جداول مشفّرة**. الواقع في `src/core/db/encryption.ts`: **12 جدولاً**.

أمثلة على الأخطاء:

| الادعاء | الواقع |
|---|---|
| `transactions: amount, description, category` | ❌ الحقيقة: `amount, description, notes, attachment, originalAmount, location, smsText` — **الفئة `category` ليست مشفّرة** (لأنها فهرس Dexie) |
| `cards: numberFull, cvv, pin, holderName` | ❌ الحقيقة: `number, holder, expiry` — **الأسماء كلها مختلفة، و`cvv` و`pin` غير مشفّرين لأنهما غير موجودين بهذه الأسماء** |
| `goals: targetAmount, currentAmount, name` | ❌ الحقيقة: `targetAmount, saved, name, target` |
| `debts: amount, paid, name` | ناقص: `notes, total` |
| — | ❌ **أغفلت تماماً**: `budgets`, `bills`, `subscriptions`, `installments`, `auditLog`, `chatHistory` |

> **لماذا هذا الأخطر في الوثيقة كلها:** ادعاء أن `cvv` و`pin` مشفّران بينما لا وجود لهما بهذا الاسم في خريطة التشفير هو **ادعاء أمني غير مدعوم**. لو اعتمد مدقق خارجي أو عميل على هذا الجدول لاستنتج ضمانات غير قائمة. يجب تصحيح هذا الجدول فوراً ونسخه حرفياً من الكود.

---

### 🔴 خطأ ٣ — فصل التوجيه: العدد والمسارات والأسماء

الفصل 40 عنوانه "32 مسار". العدد الفعلي في `src/AppRoot.tsx`: **38 مسار `<Route>`**.

**مسارات موجودة في الكود وغائبة تماماً عن الوثيقة (٦):**
`/audit` · `/notifications` · `/travel-budget` · `/add-transaction` · `/vat-calculator` · (و`*` مكرر)

**ومسار خاطئ الاسم:** الوثيقة تقول `/chat`، والكود يقول **`/chatbot`** (السطر 151). هذا خطأ عملي مباشر — أي رابط داخلي مبني على الوثيقة سيؤدي إلى صفحة 404.

كذلك: "AppRoot.tsx — 191 سطر" والواقع **190 سطر** (فارق طفيف مقبول).

---

### 🔴 خطأ ٤ — لوحات الألوان: العدد والتسمية

الفصل 38.2 يقول "16 لوحة"، ثم يسرد **8 فاتحة + 9 داكنة = 17 عنصراً** — تناقض داخلي في الوثيقة نفسها.

والواقع من `GeneralSettingsCard.tsx`:

| | الوثيقة | الواقع |
|---|---|---|
| فاتحة | 8 (تشمل **Ocean**) | **8**: default, soft, cool, sepia, **nature**, rose, lavender, sage |
| داكنة | 9 | **9**: dim, midnight, oled, vantablack, royal-gold, **neon**, aurora, crimson, forest |

- ❌ **"Ocean" لا وجود لها إطلاقاً** في الكود — مخترَعة.
- ❌ أغفلت **"Nature"** (فاتحة) و**"Neon Night"** (داكنة) الموجودتين فعلاً.
- ❌ "Default Dark" و"Black" غير دقيقين؛ الاسم الفعلي للأولى `dim`.

العدد الصحيح: **17 لوحة** (8 + 9).

---

### 🔴 خطأ ٥ — إغفال كامل لأخطر مشكلة في المشروع

الفصل 52.1 يصف `android/app/build.gradle` بتفصيل (versionCode، minSdk، التوقيع الشرطي) **وكأنه موجود وسليم**.

الحقيقة وقت كتابة الوثيقة: **ملفات Gradle الستة كانت مفقودة بالكامل من المستودع، والتطبيق كان غير قابل للبناء على أندرويد إطلاقاً.** (أُصلحت في commit `ee272e9` بعد أن أرسلتَها.)

وأيضاً: الوثيقة تقول `versionCode: 2306`، والقيمة الفعلية في الملف **`230006`**.

وتقول: *"Firebase: تضمين شرطي لـ google-services.json لتفعيل FCM و Crashlytics"* — وهذا **غير صحيح**: `app/build.gradle` لا يحتوي على أي `apply plugin: 'com.google.gms.google-services'` (تحققتُ: صفر تطابقات)، وملف `google-services.json` غير موجود. **Crashlytics لن يعمل** رغم أن الكود يستدعيه في 10 ملفات.

---

## 2. أخطاء متوسطة

| # | الموضع | الادعاء | الواقع |
|---|---|---|---|
| 6 | 2.2 | 39 حزمة إنتاجية | **38** |
| 7 | 2.5 | TypeScript Target: **ES2020** | **ES2022** |
| 8 | 2.4 | وكيل `/api → localhost:3000` | ❌ غير موجود. الوكلاء الفعليون: `/google-ai`, `/api/fred`, وغيرها |
| 9 | 2.4 | chunks: `vendor-zustand`, `vendor-dexie`, `vendor-firebase`, `vendor-xlsx` | ❌ **أربعتها غير موجودة**. zustand مدموج في `vendor-react`، ولا توجد حزمة xlsx ولا firebase-js أصلاً |
| 10 | 35.1 | 409 اختبار / 69 جناح | **413 اختبار / 70 جناح** (بعد إضافة `lockLifecycle.test.ts`) |
| 11 | 35.3 | 59 ملف وحدة | **60** |
| 12 | 35.4 | "5 ملفات مكونات مدمجة" | ❌ مجلد `tests/components/` **غير موجود**؛ الإجمالي 65 ملفاً (60 unit + 5 integration) |
| 13 | 6.1 | crypto.ts = 156 سطر | **186** |
| 14 | 6.1 | encryption.ts = 169 سطر | **188** |
| 15 | 6.1 | secureStore.ts ≈ 200 | **232** |
| 16 | 33.1 | 3,100 مفتاح ترجمة | **~3,087** (تقريب مقبول لكن يُفضّل الدقة) |
| 17 | 28.3 | مجموع أسطر الألعاب 11,200 | **11,777** (بما فيها ArcadeHub) |
| 18 | 49.2 | "2,079 ترجمة" (189 × 11) | حسابياً صحيح، لكنه يفترض تغطية 100% لم أتحقق منها لكل لغة |

**ملاحظة إنصاف:** فروق الأسطر في الألعاب كلها **سطر واحد بالضبط** (`CoinMerger` 407 مقابل 406 فعلياً…) — هذا ليس خطأ حقيقياً بل اختلاف في عدّ السطر الأخير. لا يستحق التصحيح.

---

## 3. ادعاءات صحيحة الشكل، مضلِّلة المضمون (الأهم)

هذا القسم أهم من قائمة الأخطاء، لأن كل بند فيه **صحيح حرفياً وخاطئ عملياً**.

### ⚠️ أ — "التشفير المغلّف (Envelope Encryption)"

الوثيقة (6.2) تقول:
> *"يُنشأ مفتاح بيانات عشوائي (DEK)… هذا يسمح بتغيير PIN دون إعادة تشفير كل البيانات"*

**الواقع:** دوال المغلّف موجودة في `crypto.ts` (`Generate MDK`, `wrap with KEK`, `unwrap`) لكنني بحثتُ في كل المستودع: **لا أحد يستدعيها**. صفر استدعاءات خارج ملف تعريفها.

النتيجة العملية: **تغيير الـ PIN يتطلب فعلياً إعادة تشفير كل البيانات.** الوثيقة تَعِد بخاصية غير مُفعّلة.

✅ **الصواب:** «البنية التحتية للتشفير المغلّف جاهزة في `crypto.ts` لكنها غير مُوصَّلة بعد؛ تغيير PIN حالياً يستلزم إعادة تشفير كاملة.»

### ⚠️ ب — "كشف الروت" و"مكافحة التلاعب"

الوثيقة (6.5) تعرضهما كضمانات أمنية. الواقع في `security.ts` سطر 149:

```js
if (ua.includes('test-keys') || ua.includes('superus') || ua.includes('rooted'))
```

هذا **فحص نصّي على User-Agent** — يتجاوزه أي مستخدم روت حقيقي بلا جهد، ولا يكتشف Magisk أو أي أداة حديثة. تسميته "كشف روت" في مرجع رسمي مبالغة.

✅ **الصواب:** «فحص استرشادي سطحي على User-Agent، ليس كشف روت حقيقياً ولا يُعتمد عليه أمنياً.»

### ⚠️ ج — "5 محاولات خاطئة: حظر مؤقت"

صحيح أن `MAX_ATTEMPTS = 5` موجود. لكن `attempts` و`lockedUntil` مخزَّنان في **`useState` فقط** (`PinScreen.tsx` سطر 48-49) — أي أن **إغلاق التطبيق وإعادة فتحه يصفّر العداد فوراً**. الحماية من التخمين شبه معدومة عملياً.

### ⚠️ د — "درع الخصوصية يمنع لقطات الشاشة"

الوثيقة (6.4) تقول إن تراكب الضباب *"يمنع التقاط لقطات شاشة"*. الضباب CSS لا يمنع لقطة شاشة إطلاقاً. المنع الحقيقي يأتي من `FLAG_SECURE` — الذي **لم يكن مُفعّلاً** وأضفتُه في commit `ecab2c2`. الآن صار الادعاء صحيحاً، لكن **لأننا أصلحنا الكود، لا لأن الوثيقة كانت دقيقة**.

### ⚠️ هـ — "كشف المحاكيات… لمنع التشغيل على محاكيات"

الكود يُظهر `toast` تحذيرياً فقط ويُسجّل الحدث. **لا يمنع التشغيل.** عبارة "لمنع التشغيل" غير صحيحة.

---

## 4. ملاحظات على الشكل والصياغة

### 🔗 122 رابطاً مكسوراً

الوثيقة تحتوي **122 رابطاً** بالصيغة:

```
file:///c:/Users/moham/Downloads/TEST/src/core/security.ts
```

هذه مسارات على جهاز الكاتب الشخصي. **لن تعمل عند أي قارئ آخر**، وتكشف اسم المستخدم ومسار المجلد (تسريب معلومات بسيط). كذلك اسم المجلد `TEST` يوحي بأن الوثيقة كُتبت من نسخة تجريبية لا من المستودع الرسمي.

✅ **الإصلاح:** استبدلها بمسارات نسبية:
```markdown
[security.ts](src/core/security.ts)
```
أمر واحد يكفي:
```bash
sed -i 's|file:///c:/Users/moham/Downloads/TEST/||g' masarifi_reference_book.md
```

### 📄 مشكلة في مكان الملف

رفعتَ الملف على فرع **`main`** مباشرة، بينما كل عملنا على `arena/01a097d5-msasafi`. هذا يعني أن الفرعين تباعدا. أنصح بنقله إلى فرع العمل لتبقى الوثيقة والكود متزامنين.

### 🗂️ تكرار مع وثائق قائمة

المستودع يحتوي الآن **أربع وثائق متداخلة**:

| الملف | الحجم |
|---|---|
| `masarifi_reference_book.md` | 2602 سطر |
| `GEMINI.md` | 1236 سطر / 235 ك.ب |
| `PROJECT_DOCUMENTATION.md` | 259 سطر |
| `AUDIT_REPORT.md` | 602 سطر |

أربع مصادر للحقيقة = أربع فرص للتناقض. أنصح بدمج `PROJECT_DOCUMENTATION.md` داخل المرجع الجديد وحذفه.

---

## 5. ما هو صحيح تماماً (تحقّقتُ منه)

إنصافاً للوثيقة، هذه الادعاءات **مطابقة للكود بالضبط**:

| الادعاء | التحقق |
|---|---|
| 27 ودجة في الشاشة الرئيسية | ✅ مطابق تماماً لـ `homeOrder` |
| 25 ملف أنواع TypeScript | ✅ 25 |
| 6 متاجر حالة | ✅ 6 |
| 24 لعبة + ArcadeHub | ✅ 25 ملفاً = 24 لعبة + الهَب |
| 189 نصيحة، `chat.tip.0` → `chat.tip.188` | ✅ مطابق |
| 11 لغة (10 ملفات + العربية مضمّنة) | ✅ مطابق |
| 13 آلة حاسبة | ✅ 13 قسماً |
| PBKDF2 600,000 دورة | ✅ مطابق في `crypto.ts` و`secureStore.ts` |
| AES-GCM 256 + IV 12 بايت | ✅ صحيح |
| appId = `com.masarifi.app` | ✅ |
| versionName 23.0.6 | ✅ مطابق لـ `package.json` |
| minSdk 24 / compile & target 36 | ✅ |
| 32 حزمة تطوير | ✅ 32 |
| React 19 / Vite 6 / Capacitor 8 / Zustand 5 / Dexie 4 / Tailwind 4 | ✅ كلها صحيحة |
| Gemini 1.5 Flash كمحرك أساسي | ✅ |
| 7 ملفات في `src/core/ai/` | ✅ (+ `index.ts`) |
| أعداد أسطر الألعاب الـ24 | ✅ بفارق سطر واحد فقط |

---

## 6. اقتراحات لتطوير الوثيقة

### 1️⃣ أضف فصلاً للقيود والمشاكل المعروفة ← *الأهم*

مرجع بلا هذا الفصل يُعطي ثقة زائفة. يجب أن يذكر صراحةً:
- ✅ ملفات Gradle (كانت مفقودة — أُصلحت)
- ❌ Crashlytics غير مُفعّل (ينقص `apply plugin` + `google-services.json`)
- ❌ التشفير المغلّف غير موصَّل
- ❌ عدّاد محاولات PIN يُصفَّر عند إعادة التشغيل
- ❌ كشف الروت سطحي
- ❌ 15 ملفاً يتجاوز 600 سطر (`AddTransactionPage.tsx` = 1315)
- ❌ مكونات مكررة (`ImageCropper` ×2، حاسبات ×4)

### 2️⃣ ولّد الأرقام آلياً بدل كتابتها يدوياً

كل الأخطاء الرقمية (38 مقابل 32 مساراً، 413 مقابل 409 اختباراً…) سببها واحد: **أرقام مكتوبة يدوياً تتقادم مع أول commit**. اكتب سكربتاً في `scripts/` يستخرجها ويحدّث الوثيقة، وشغّله في CI.

أستطيع كتابته لك إن أردت.

### 3️⃣ ثبّت رقم الإصدار و SHA

أضف في الترويسة: `يوافق commit: ee272e9`. بلا ذلك لا يعرف القارئ أي نسخة يصف.

### 4️⃣ افصل "موجود" عن "مخطط له"

ميزات العائلة وحسابات الأطفال موصوفة بتفصيل بلا جداول في المخطط. استخدم وسماً: ✅ مُنفَّذ · 🚧 جزئي · 📋 مخطط.

### 5️⃣ أصلح الروابط المكسورة الـ122 (أمر `sed` أعلاه)

---

## 7. خلاصة تنفيذية

| المحور | التقييم |
|---|---|
| الشمول والتنظيم | ⭐⭐⭐⭐⭐ ممتاز — 52 فصلاً بترتيب منطقي |
| دقة الأرقام التفصيلية | ⭐⭐⭐⭐ جيدة جداً (أعداد الأسطر والودجات مضبوطة) |
| دقة البنية (جداول/مسارات) | ⭐⭐ ضعيفة — 6 جداول و7 مسارات خاطئة |
| **دقة الادعاءات الأمنية** | ⭐⭐ **ضعيفة — تصف نوايا لا واقعاً** |
| الشفافية عن القيود | ⭐ **معدومة — لا ذكر لأي مشكلة** |
| جاهزية الروابط | ⭐ 122 رابطاً مكسوراً |

**التوصية:** الوثيقة أساس ممتاز يستحق الإصلاح لا الاستبدال. أولويات التصحيح بالترتيب:

1. **جدول `ENCRYPTED_FIELDS`** (خطأ أمني — انسخه حرفياً من الكود)
2. **قائمة جداول قاعدة البيانات** (6 أسماء خاطئة)
3. **قائمة المسارات** (38 لا 32، و`/chatbot` لا `/chat`)
4. **فصل القيود والمشاكل المعروفة** (غائب كلياً)
5. الروابط الـ122
6. الأرقام المتفرقة

**تنبيه أخير:** لم أراجع صحة المعادلات الرياضية (الإهلاك، EMI، Z-Score، الزكاة) مقابل تنفيذها في الكود — تتطلب قراءة `assetsEngine.ts` و`calcEngine.ts` سطراً بسطر. أخبرني إن أردت ذلك في جولة تالية، فحساب الزكاة تحديداً له حساسية شرعية تستحق تدقيقاً منفصلاً.
